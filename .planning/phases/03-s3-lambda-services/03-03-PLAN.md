---
phase: 03-s3-lambda-services
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - web/src/services/s3/S3Layout.tsx
  - web/src/services/s3/BucketListPage.tsx
  - web/src/services/s3/components/BucketTable.tsx
  - web/src/services/s3/components/BucketDetail.tsx
  - web/src/services/s3/components/CreateBucketModal.tsx
  - web/src/services/s3/components/DeleteBucketModal.tsx
  - web/src/services/s3/components/columns.ts
  - web/src/shared/copy.ts
  - web/src/stores/uiStore.ts
  - web/src/app/routes.tsx
autonomous: true
requirements:
  - S3-01

must_haves:
  truths:
    - "User navigates to /services/s3 and sees a Table of buckets (from MSW/backend) with Name, Creation date, Region columns"
    - "User clicks bucket name Link → URL becomes /services/s3/{bucketName} (ObjectBrowserPage stub renders placeholder until Plan 04)"
    - "User clicks a bucket row (non-name cell) → SplitPanel opens showing bucket properties"
    - "User clicks Create bucket → Modal opens, name validation displays specific error per rule, successful submit creates bucket and refreshes list with Flashbar success"
    - "User clicks Delete bucket → Type-to-confirm modal requires exact bucket-name match; BucketNotEmpty error surfaces as Flashbar with D-12 copy"
    - "S3 routes sit BEFORE services/:serviceKey wildcard in routes.tsx (Pitfall 5)"
    - "Refresh button invalidates ['s3','buckets'] query"
  artifacts:
    - path: "web/src/services/s3/S3Layout.tsx"
      provides: "Page wrapper for S3 routes (SplitPanel provider wiring, shared Header)"
      exports: ["default"]
    - path: "web/src/services/s3/BucketListPage.tsx"
      provides: "/services/s3 route component"
      exports: ["default"]
    - path: "web/src/services/s3/components/BucketTable.tsx"
      provides: "Cloudscape Table for bucket list with TextFilter, Pagination, CollectionPreferences"
      exports: ["BucketTable"]
    - path: "web/src/services/s3/components/BucketDetail.tsx"
      provides: "SplitPanel content for selected bucket"
      exports: ["BucketDetail"]
    - path: "web/src/services/s3/components/CreateBucketModal.tsx"
      provides: "Create bucket form modal (name validation + inline Alert)"
      exports: ["CreateBucketModal"]
    - path: "web/src/services/s3/components/DeleteBucketModal.tsx"
      provides: "Single + bulk delete confirmation modal (type-to-confirm)"
      exports: ["DeleteBucketModal"]
    - path: "web/src/services/s3/components/columns.ts"
      provides: "Table column definitions for buckets (and exports object columns for Plan 04)"
      exports: ["BUCKET_COLUMNS", "BUCKET_VISIBLE_CONTENT"]
    - path: "web/src/shared/copy.ts"
      provides: "EXTEND: add s3.* copy entries"
    - path: "web/src/stores/uiStore.ts"
      provides: "EXTEND: s3BucketPageSize + s3ObjectPageSize"
    - path: "web/src/app/routes.tsx"
      provides: "EXTEND: insert S3 routes BEFORE services/:serviceKey"
  key_links:
    - from: "web/src/services/s3/BucketListPage.tsx"
      to: "web/src/services/s3/api/useBuckets.ts"
      via: "useBuckets()"
      pattern: "useBuckets"
    - from: "web/src/services/s3/components/BucketTable.tsx"
      to: "react-router-dom"
      via: "Link to /services/s3/{name}"
      pattern: "to=.*services/s3"
    - from: "web/src/app/routes.tsx"
      to: "web/src/services/s3/S3Layout.tsx"
      via: "lazy import"
      pattern: "services/s3"
---

<objective>
Implement the S3 bucket list page: route wiring, page shell, bucket table with navigation links, create/delete modals, bucket detail SplitPanel. Satisfies S3-01 (list/create/delete buckets) end-to-end. Leaves `/services/s3/:bucketName` wired to a placeholder `ObjectBrowserPage` that Plan 04 will flesh out.

Purpose: First fully interactive S3 surface. Validates API assumptions (A1, A7) against the live backend: a user can see buckets listed, create one, and delete one from this page alone. Proves the routing pattern and locks in the copy catalog.
Output: 7 new files + 3 EXTENDED files. S3-01 requirement complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-s3-lambda-services/03-CONTEXT.md
@.planning/phases/03-s3-lambda-services/03-UI-SPEC.md
@.planning/phases/03-s3-lambda-services/03-RESEARCH.md
@.planning/phases/03-s3-lambda-services/03-02-SUMMARY.md
@web/src/app/routes.tsx
@web/src/shared/copy.ts
@web/src/stores/uiStore.ts
@web/src/services/ec2/components/ResourceTable.tsx
@web/src/services/ec2/components/DeleteModal.tsx
@web/src/services/ec2/components/CreateModal.tsx
@web/src/services/ec2/components/FlashNotifications.tsx
@web/src/services/ec2/components/SplitPanelDetail.tsx
@web/src/contexts/SplitPanelContext.tsx

<interfaces>
From Plan 02:
  useBuckets(): UseQueryResult<S3Bucket[]>
  useCreateBucket(): UseMutationResult<void, Error, string>
  useDeleteBucket(): UseMutationResult<void, Error & {code?: string}, string>

From Plan 01:
  validateBucketName(name: string): string | null
  type S3Bucket = { name: string; creationDate: string }

From existing code:
  web/src/services/ec2/components/ResourceTable.tsx — generic Table wrapper (props: items, columnDefinitions, filter, selection, loading, etc.)
  web/src/services/ec2/components/DeleteModal.tsx — type-to-confirm modal (props: resourceType, resourceId, onDelete, onDismiss)
  web/src/services/ec2/components/CreateModal.tsx — generic create modal wrapper
  web/src/services/ec2/components/FlashNotifications.tsx — useFlashNotifications() returns {items, pushSuccess, pushError, pushInfo}
  web/src/services/ec2/components/SplitPanelDetail.tsx — SplitPanelDetail({header, tabs?, keyValueItems?})
  web/src/contexts/SplitPanelContext.tsx — useSplitPanel() {setContent, setOpen} — wires into AppLayout splitPanel slot

Phase 2 component reuse policy (per Research Assumption A6): Import EC2 components DIRECTLY from their current path (../../ec2/components/...). Do NOT extract to services/shared/ — that's a Phase 4 refactor per RESEARCH.md anti-pattern. If a component has EC2-specific copy or logic that breaks reuse, create a S3-local variant with same name in services/s3/components/ and note the divergence in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend routes + copy + uiStore; build S3Layout and BucketListPage shell</name>
  <files>web/src/app/routes.tsx, web/src/shared/copy.ts, web/src/stores/uiStore.ts, web/src/services/s3/S3Layout.tsx, web/src/services/s3/BucketListPage.tsx, web/src/services/s3/components/columns.ts</files>
  <behavior>
    - `/services/s3` renders BucketListPage; `/services/s3/:bucketName` renders a placeholder ObjectBrowserPage (lazy-imported; Plan 04 implements). Both routes placed BEFORE `services/:serviceKey` wildcard (Pitfall 5).
    - copy.ts extended with an `s3` namespace containing every copy string catalogued in UI-SPEC §"Copywriting Contract".
    - uiStore.ts extended with `s3BucketPageSize` (default 10) and `s3ObjectPageSize` (default 50) persisted under existing `ministack:console` storage key.
  </behavior>
  <read_first>
    - web/src/app/routes.tsx (current route table; see line containing `services/:serviceKey` — new routes must come before)
    - web/src/shared/copy.ts (existing copy namespaces — mirror pattern, do NOT replace or reorder existing entries)
    - web/src/stores/uiStore.ts (zustand create + persist pattern)
    - .planning/phases/03-s3-lambda-services/03-UI-SPEC.md §"Copywriting Contract" (exact copy strings)
    - .planning/phases/03-s3-lambda-services/03-UI-SPEC.md §"Bucket List Contract" (table config)
  </read_first>
  <action>
    **web/src/stores/uiStore.ts (EXTEND)**:
    Add to UiState type and `create` body:
      `s3BucketPageSize: number; setS3BucketPageSize: (size: number) => void`
      `s3ObjectPageSize: number; setS3ObjectPageSize: (size: number) => void`
    Defaults: 10 and 50 respectively. Keep existing persist name `ministack:console` — zustand merges keys.

    **web/src/shared/copy.ts (EXTEND)**:
    Append a nested `s3` object at the end of the `copy` literal (before the closing `}`) containing every string from UI-SPEC §"Copywriting Contract" subsections: Page-Level, Bucket List, Create Bucket Modal, Delete Bucket, Object Browser, Upload, Download, Object Detail, Object Delete, Miscellaneous. Use functions for dynamic entries (e.g., `deleteBucketBody: (bucketName: string) => \`Are you sure you want to delete bucket ${bucketName}? ...\``). Count: approximately 70 entries total. Must match UI-SPEC text VERBATIM (tone rules: imperative voice, no emoji, AWS canonical capitalization).

    **web/src/services/s3/components/columns.ts**:
    Export `BUCKET_COLUMNS: TableProps.ColumnDefinition<S3Bucket>[]` with 3 columns matching UI-SPEC Bucket List table spec:
    - Name — `cell: (b) => <Link to={'/services/s3/' + b.name}>{b.name}</Link>`, sortingField: 'name', minWidth: 240
    - Creation date — `cell: (b) => b.creationDate`, sortingField: 'creationDate', minWidth: 200
    - Region — `cell: () => copy.region` (static 'us-east-1'), minWidth: 120
    Also export `BUCKET_VISIBLE_CONTENT = ['name', 'creationDate', 'region']`.

    **web/src/services/s3/S3Layout.tsx**:
    ```tsx
    export default function S3Layout() { return <Outlet /> }
    ```
    Minimal wrapper — matches Phase 2's `Ec2Dashboard` pattern for shared route wrapping. Header rendering happens in each page.

    **web/src/services/s3/BucketListPage.tsx**:
    Render:
    - Cloudscape `Header variant="h1"` with title `copy.s3.serviceHeading` and description `copy.s3.serviceDescription`
    - `BreadcrumbGroup` items: `[Console, S3]` (use existing app breadcrumb approach from Phase 2)
    - `<BucketTable />` placeholder for Task 2. Until Task 2 lands, render a Box with loading state using `useBuckets()` (so Task 1 can be committed and verified independently).

    **web/src/app/routes.tsx (EXTEND)**:
    Add `const S3Layout = lazy(() => import('../services/s3/S3Layout'))`, `const BucketListPage = lazy(() => import('../services/s3/BucketListPage'))`, `const ObjectBrowserPage = lazy(() => import('../services/s3/ObjectBrowserPage'))` (the last file will be stubbed in this task too).

    Insert BEFORE the `{ path: 'services/:serviceKey', ... }` route:
    ```tsx
    {
      path: 'services/s3',
      element: withSuspense(<S3Layout />),
      children: [
        { index: true, element: withSuspense(<BucketListPage />) },
        { path: ':bucketName', element: withSuspense(<ObjectBrowserPage />) },
      ],
    },
    ```

    Create a minimal `web/src/services/s3/ObjectBrowserPage.tsx` stub that renders `<Header>{bucketName}</Header>` + a Box saying "Object browser coming in Plan 04" (so routes.tsx compiles and routing is testable now). Plan 04 replaces this file.

    **Do NOT implement BucketTable / modals yet** — that's Task 2.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json && npm run test -- src/app/__tests__/routes --run 2>&1 || cd web && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "services/s3" web/src/app/routes.tsx` >= 2
    - `grep -n "services/s3" web/src/app/routes.tsx` shows S3 path line number LESS than `services/:serviceKey` line number (Pitfall 5)
    - `grep -c "s3BucketPageSize\|s3ObjectPageSize" web/src/stores/uiStore.ts` >= 4
    - `grep -c "s3:" web/src/shared/copy.ts` >= 1  (the s3 namespace exists)
    - `grep -cE "(serviceHeading|bucketsEmptyHeading|createBucketButton|uploadButton|deleteBucketHeader)" web/src/shared/copy.ts` >= 5
    - `web/src/services/s3/S3Layout.tsx`, `BucketListPage.tsx`, `ObjectBrowserPage.tsx`, `components/columns.ts` all exist
    - `grep -c "BUCKET_COLUMNS\|BUCKET_VISIBLE_CONTENT" web/src/services/s3/components/columns.ts` == 2
    - `cd web && npx tsc --noEmit -p tsconfig.json` zero errors
    - Manual smoke: `cd web && npm run dev` → visiting `/_console/services/s3` renders BucketListPage heading (not ServiceHome placeholder)
  </acceptance_criteria>
  <done>Routes, copy, uiStore extended per decisions. S3Layout + BucketListPage + stub ObjectBrowserPage exist. Project compiles and routing reaches the S3 page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement BucketTable, BucketDetail, CreateBucketModal, DeleteBucketModal and wire into BucketListPage</name>
  <files>web/src/services/s3/components/BucketTable.tsx, web/src/services/s3/components/BucketDetail.tsx, web/src/services/s3/components/CreateBucketModal.tsx, web/src/services/s3/components/DeleteBucketModal.tsx, web/src/services/s3/BucketListPage.tsx</files>
  <behavior>
    - BucketTable: Cloudscape Table with selectionType="multi", TextFilter, Pagination, CollectionPreferences (page sizes 10/25/50), Header with "Buckets (N)" + Refresh + Create bucket + ButtonDropdown(Delete). Uses `useCollection` from @cloudscape-design/collection-hooks. Page size persisted via `useUiStore.s3BucketPageSize`.
    - BucketDetail: KeyValuePairs in SplitPanel showing Name, Region, Creation date for the selected bucket.
    - CreateBucketModal: FormField with live validation via `validateBucketName`; submit button disabled until valid; on server error, inline Alert at top of Form; on success, closes + pushSuccess Flashbar + invalidate cascade via useCreateBucket.
    - DeleteBucketModal: supports single (type bucket name) and bulk (type "delete") — decide by `buckets.length`; on BucketNotEmpty error, close modal and pushError Flashbar with exact copy from UI-SPEC §Delete Bucket.
    - BucketListPage wires everything together and routes SplitPanel content to `useSplitPanel()`.
  </behavior>
  <read_first>
    - web/src/services/ec2/components/ResourceTable.tsx (generic Table wrapper — prop shape)
    - web/src/services/ec2/components/DeleteModal.tsx (type-to-confirm pattern to mirror)
    - web/src/services/ec2/components/CreateModal.tsx (Form+Alert wrapper pattern)
    - web/src/services/ec2/components/FlashNotifications.tsx (useFlashNotifications hook)
    - web/src/services/ec2/components/SplitPanelDetail.tsx (SplitPanelDetail props)
    - web/src/contexts/SplitPanelContext.tsx (useSplitPanel())
    - .planning/phases/03-s3-lambda-services/03-UI-SPEC.md §"Bucket List Contract", §"Create Bucket Modal", §"Delete Bucket Contract"
    - web/src/services/s3/components/BucketTable.test.tsx, CreateBucketModal.test.tsx, DeleteBucketModal.test.tsx (Plan 00 stubs — replace test.todo with real tests)
  </read_first>
  <action>
    **BucketTable.tsx**:
    - Props: `{ buckets: S3Bucket[]; loading: boolean; error: Error | null; onRefresh: () => void; onCreate: () => void; onDeleteSelected: (buckets: S3Bucket[]) => void; onSelectionChange: (selected: S3Bucket | null) => void }`
    - Use `useCollection(buckets, { filtering: {}, pagination: { pageSize }, sorting: { defaultState: { sortingColumn: BUCKET_COLUMNS[0] } } })` with pageSize from `useUiStore(s => s.s3BucketPageSize)`.
    - Header with `counter={\`(${buckets.length})\`}`, actions: Refresh icon button, Create bucket primary button, ButtonDropdown "Actions" with Delete (disabled when no selection).
    - Empty state: use copy.s3.bucketsEmptyHeading / ...Body / ...Cta (Create bucket → calls onCreate).
    - Load error: Cloudscape Alert with copy.s3.bucketsLoadError* and a Retry button calling onRefresh.
    - `onSelectionChange`: single-selection semantics for detail panel — pass first item or null. Bulk delete still reads multi-selection via `selectedItems`.
    - Bucket name cell renders `<Link to={\`/services/s3/${b.name}\`}>{b.name}</Link>` so clicking name navigates (not toggles selection).

    **BucketDetail.tsx**:
    ```tsx
    export function BucketDetail({ bucket }: { bucket: S3Bucket }) {
      return <SplitPanelDetail header={bucket.name} keyValueItems={[
        { label: copy.s3.bucketPropertyName, value: bucket.name },
        { label: copy.s3.bucketPropertyRegion, value: copy.region },
        { label: copy.s3.bucketPropertyCreationDate, value: bucket.creationDate },
      ]} />
    }
    ```

    **CreateBucketModal.tsx**:
    - Props: `{ visible: boolean; onDismiss: () => void; onCreated: (name: string) => void }`
    - State: `name`, `touched`. Compute `validationError = validateBucketName(name)`.
    - Render Modal with Form > FormField (label copy.s3.createBucketNameLabel, description copy.s3.createBucketNameHelp, errorText = touched ? validationError : null) + Input (placeholder copy.s3.createBucketNamePlaceholder).
    - If `mutation.isError`, show Alert type="error" at top of Form with the error message.
    - Submit button disabled when validationError !== null or mutation.isPending.
    - On submit: `useCreateBucket().mutate(name, { onSuccess: () => { onCreated(name); onDismiss() } })`.

    **DeleteBucketModal.tsx**:
    - Props: `{ visible: boolean; buckets: S3Bucket[]; onDismiss: () => void; onDeleted: (result: { deleted: string[]; errors: {name: string; message: string}[] }) => void }`
    - Single-bucket mode (buckets.length === 1): copy.s3.deleteBucketBody(name), confirmation prompt copy.s3.deleteBucketConfirmPrompt(name), required input === bucketName exactly.
    - Bulk mode (buckets.length > 1): copy.s3.bulkDeleteBucketsBody(count), required input === "delete".
    - Delete button disabled until input matches.
    - On click Delete: sequentially call `useDeleteBucket().mutateAsync(name)` for each bucket; collect errors (specifically, detect `err.code === 'BucketNotEmpty'` and surface per UI-SPEC copy). Return aggregate to onDeleted. Close modal after completion.

    **BucketListPage.tsx** (wire everything):
    - `const { items: buckets, isLoading, error, refetch } = useBuckets()`
    - `const { items: flashItems, pushSuccess, pushError } = useFlashNotifications()`
    - `const { setContent, setOpen } = useSplitPanel()`
    - `const [createOpen, setCreateOpen] = useState(false); const [deleteTargets, setDeleteTargets] = useState<S3Bucket[] | null>(null)`
    - Render Flashbar at top, Header + BucketTable, CreateBucketModal, DeleteBucketModal.
    - Handlers: onCreate → setCreateOpen(true); onCreated(name) → pushSuccess(copy.s3.createBucketSuccess(name)); onDeleteSelected(list) → setDeleteTargets(list); onDeleted({deleted, errors}) → pushSuccess/pushError per UI-SPEC §"Flashbar Notification Contract" rows (including BucketNotEmpty explicit copy).
    - onSelectionChange(bucket) → `setContent(bucket ? <BucketDetail bucket={bucket} /> : null); setOpen(Boolean(bucket))`

    **Replace test.todo() in BucketTable.test.tsx, CreateBucketModal.test.tsx, DeleteBucketModal.test.tsx** with real RTL tests using MSW + s3Handlers + `QueryClientProvider` + `MemoryRouter` helpers from Phase 1's test utils.
  </action>
  <verify>
    <automated>cd web && npm run test -- src/services/s3/components/BucketTable src/services/s3/components/CreateBucketModal src/services/s3/components/DeleteBucketModal --run</automated>
  </verify>
  <acceptance_criteria>
    - 4 new component files exist
    - `grep -c "useCollection" web/src/services/s3/components/BucketTable.tsx` >= 1
    - `grep -c "selectionType=\"multi\"\|selectionType={'multi'}" web/src/services/s3/components/BucketTable.tsx` >= 1
    - `grep -c "validateBucketName" web/src/services/s3/components/CreateBucketModal.tsx` >= 1
    - `grep -c "useCreateBucket" web/src/services/s3/components/CreateBucketModal.tsx` >= 1
    - `grep -c "useDeleteBucket" web/src/services/s3/components/DeleteBucketModal.tsx` >= 1
    - `grep -cE "BucketNotEmpty|bucket is not empty" web/src/services/s3/components/DeleteBucketModal.tsx web/src/services/s3/BucketListPage.tsx` >= 1
    - `grep -c "useSplitPanel" web/src/services/s3/BucketListPage.tsx` >= 1
    - `grep -c "useFlashNotifications" web/src/services/s3/BucketListPage.tsx` >= 1
    - Zero `test.todo` remain across BucketTable.test.tsx, CreateBucketModal.test.tsx, DeleteBucketModal.test.tsx
    - `cd web && npm run test -- src/services/s3/components/BucketTable src/services/s3/components/CreateBucketModal src/services/s3/components/DeleteBucketModal --run` exits 0
    - `cd web && npx tsc --noEmit -p tsconfig.json` zero errors
  </acceptance_criteria>
  <done>Bucket list page fully interactive: list/create/delete buckets works against MSW handlers; SplitPanel opens on row click; Flashbar surfaces per-decision messages; all component test stubs converted to real passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Create Bucket modal input → backend | User-supplied bucket names |
| Delete Bucket type-to-confirm → mutation | Destructive action gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03-01 | Tampering (V5 Input Validation) | CreateBucketModal | mitigate | validateBucketName() runs on every change; submit disabled while invalid; server re-validates via `_validate_bucket_name`. |
| T-3-03-02 | Business Logic (V11) | DeleteBucketModal | mitigate | Type-to-confirm with exact bucket-name match (single) or literal "delete" (bulk). Disables Delete until match. |
| T-3-03-03 | XSS via error message | BucketListPage Flashbar | mitigate | All server error strings rendered via React `{error.message}`, no dangerouslySetInnerHTML (grep confirms). |
| T-3-03-04 | Tampering (route injection) | routes.tsx ordering | mitigate | S3 routes inserted BEFORE services/:serviceKey (Pitfall 5) — grep check in acceptance_criteria. |
</threat_model>

<verification>
- `cd web && npm run test -- src/services/s3 --run` all tests pass, zero todos remain in bucket-related tests
- `cd web && npx tsc --noEmit -p tsconfig.json` zero errors
- Manual smoke (live backend): visit /_console/services/s3 → see bucket list; create "test-bucket-$(date +%s)" → appears; delete → disappears; try deleting a non-empty bucket → BucketNotEmpty flash appears with exact UI-SPEC copy
- `grep -rn "dangerouslySetInnerHTML" web/src/services/s3/` returns no matches
</verification>

<success_criteria>
S3-01 satisfied end-to-end: users list/create/delete buckets with full validation, type-to-confirm, and error feedback. Route ordering verified. Copy matches UI-SPEC verbatim. Reused Phase 2 components imported without duplication.
</success_criteria>

<output>
After completion, create `.planning/phases/03-s3-lambda-services/03-03-SUMMARY.md`
</output>
