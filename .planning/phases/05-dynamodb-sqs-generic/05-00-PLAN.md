---
phase: 05-dynamodb-sqs-generic
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - web/src/test/fixtures/ddb.ts
  - web/src/test/fixtures/sqs.ts
  - web/src/test/fixtures/iam.ts
  - web/src/test/fixtures/sts.ts
  - web/src/test/fixtures/kms.ts
  - web/src/test/fixtures/secretsmanager.ts
  - web/src/test/fixtures/ssm.ts
  - web/src/services/ddb/__tests__/msw-handlers.ts
  - web/src/services/sqs/__tests__/msw-handlers.ts
  - web/src/services/_generic/__tests__/msw-handlers.ts
  - web/src/services/_generic/types.test.ts
  - web/src/services/_generic/registry.test.ts
  - web/src/services/_generic/keys.test.ts
  - web/src/services/_generic/adapters/awsJson.test.ts
  - web/src/services/_generic/adapters/awsQuery.test.ts
  - web/src/services/_generic/adapters/rest.test.ts
  - web/src/services/_generic/adapters/buildRequest.test.ts
  - web/src/services/_generic/hooks/useGenericList.test.ts
  - web/src/services/_generic/hooks/useGenericItem.test.ts
  - web/src/services/_generic/hooks/useGenericMutation.test.ts
  - web/src/services/_generic/GenericServiceRouter.test.tsx
  - web/src/services/_generic/GenericListPage.test.tsx
  - web/src/services/_generic/GenericDetailPanel.test.tsx
  - web/src/services/_generic/GenericCreateModal.test.tsx
  - web/src/services/_generic/GenericDeleteModal.test.tsx
  - web/src/services/_generic/GenericDiffPreviewModal.test.tsx
  - web/src/services/_generic/columnBuilders.test.ts
  - web/src/services/_generic/xmlUtils.test.ts
  - web/src/services/ddb/api/ddbClient.test.ts
  - web/src/services/ddb/api/attributeValue.test.ts
  - web/src/services/ddb/api/ddbKeys.test.ts
  - web/src/services/ddb/api/useTables.test.ts
  - web/src/services/ddb/api/useTable.test.ts
  - web/src/services/ddb/api/useScan.test.ts
  - web/src/services/ddb/api/useQuery.test.ts
  - web/src/services/ddb/api/useItem.test.ts
  - web/src/services/ddb/api/useCreateTable.test.ts
  - web/src/services/ddb/api/useDeleteTable.test.ts
  - web/src/services/ddb/api/usePutItem.test.ts
  - web/src/services/ddb/api/useUpdateItem.test.ts
  - web/src/services/ddb/api/useDeleteItem.test.ts
  - web/src/services/sqs/api/sqsClient.test.ts
  - web/src/services/sqs/api/parseSqsAttributes.test.ts
  - web/src/services/sqs/api/sqsKeys.test.ts
  - web/src/services/sqs/api/useQueues.test.ts
  - web/src/services/sqs/api/useQueueAttributes.test.ts
  - web/src/services/sqs/api/useCreateQueue.test.ts
  - web/src/services/sqs/api/useDeleteQueue.test.ts
  - web/src/services/sqs/api/useSendMessage.test.ts
  - web/src/services/sqs/api/useReceiveMessage.test.ts
  - web/src/services/sqs/api/useDeleteMessage.test.ts
  - web/src/services/sqs/api/usePurgeQueue.test.ts
  - web/src/services/iam/descriptor.users.test.ts
  - web/src/services/iam/descriptor.roles.test.ts
  - web/src/services/iam/descriptor.policies.test.ts
  - web/src/services/sts/descriptor.test.ts
  - web/src/services/secretsmanager/descriptor.test.ts
  - web/src/services/ssm/descriptor.test.ts
  - web/src/services/kms/descriptor.test.ts
autonomous: true
requirements:
  - DDB-01
  - DDB-02
  - DDB-03
  - SQS-01
  - SQS-02
  - SQS-03
  - GEN-01
  - GEN-02
  - GEN-03

must_haves:
  truths:
    - "Test stub files exist for every module named in RESEARCH §8 File Inventory under _generic/, ddb/, sqs/, and 7 descriptor files (IAM split per D-09)"
    - "MSW handlers route DDB AWS-JSON calls by X-Amz-Target header (e.g. DynamoDB_20120810.ListTables) to the correct fixture"
    - "MSW handlers route SQS AWS-JSON calls by X-Amz-Target header (e.g. AmazonSQS.ListQueues) per D-10, NOT by URLSearchParams Action parameter"
    - "MSW handlers route IAM/STS via aws-query form-encoded POST / with Action= discriminator; STS is modelled as a singleton per D-09 / §5.2"
    - "MSW handlers route Secrets Manager (X-Amz-Target: secretsmanager.*), SSM (AmazonSSM.*), KMS (TrentService.*) — note the protocol-specific prefixes"
    - "DDB Scan fixture contains S, N (string-encoded), BOOL (true + false), and NULL AttributeValues per RESEARCH §9.3 / §3"
    - "IAM and STS fixtures are valid XML strings (DOMParser-parseable) — NOT JSON"
    - "All test stubs use test.todo() — vitest reports them as todo, not failing, not passing silently"
    - "Total test.todo() count across Phase 5 stubs >= 80"
    - "Running `cd web && npm run test -- src/services/_generic src/services/ddb src/services/sqs src/services/iam src/services/sts src/services/kms src/services/secretsmanager src/services/ssm --run` exits 0 with only todo tests"
  artifacts:
    - path: "web/src/test/fixtures/ddb.ts"
      provides: "DDB JSON fixtures: ListTables, DescribeTable, Scan (S/N/BOOL/NULL), PutItem, GetItem, UpdateItem, DeleteItem"
      exports: ["DDB_FIXTURES"]
    - path: "web/src/test/fixtures/sqs.ts"
      provides: "SQS JSON fixtures: ListQueues, GetQueueAttributes, ReceiveMessage (1 msg + attrs), empty-poll"
      exports: ["SQS_FIXTURES"]
    - path: "web/src/test/fixtures/iam.ts"
      provides: "IAM XML fixtures: ListUsers, ListRoles, ListPolicies, GetUser"
      exports: ["IAM_FIXTURES"]
    - path: "web/src/test/fixtures/sts.ts"
      provides: "STS XML fixtures: GetCallerIdentity (singleton)"
      exports: ["STS_FIXTURES"]
    - path: "web/src/test/fixtures/kms.ts"
      provides: "KMS JSON fixtures: ListKeys, DescribeKey"
      exports: ["KMS_FIXTURES"]
    - path: "web/src/test/fixtures/secretsmanager.ts"
      provides: "Secrets Manager JSON fixtures: ListSecrets, DescribeSecret"
      exports: ["SECRETS_FIXTURES"]
    - path: "web/src/test/fixtures/ssm.ts"
      provides: "SSM JSON fixtures: DescribeParameters, GetParameter (SecureString masked + decrypted)"
      exports: ["SSM_FIXTURES"]
    - path: "web/src/services/ddb/__tests__/msw-handlers.ts"
      provides: "MSW handlers dispatching by X-Amz-Target for DDB actions"
      exports: ["ddbHandlers"]
    - path: "web/src/services/sqs/__tests__/msw-handlers.ts"
      provides: "MSW handlers dispatching by X-Amz-Target for SQS JSON actions (D-10)"
      exports: ["sqsHandlers"]
    - path: "web/src/services/_generic/__tests__/msw-handlers.ts"
      provides: "MSW handlers for IAM/STS (aws-query XML) + KMS/Secrets/SSM (aws-json with service-specific X-Amz-Target prefixes)"
      exports: ["genericHandlers"]
  key_links:
    - from: "web/src/services/**/*.test.ts"
      to: "web/src/test/fixtures/*.ts"
      via: "import { DDB_FIXTURES, SQS_FIXTURES, ... }"
      pattern: "DDB_FIXTURES|SQS_FIXTURES|IAM_FIXTURES|STS_FIXTURES|KMS_FIXTURES|SECRETS_FIXTURES|SSM_FIXTURES"
    - from: "web/src/services/ddb/__tests__/msw-handlers.ts"
      to: "web/src/test/fixtures/ddb.ts"
      via: "import { DDB_FIXTURES }"
      pattern: "DDB_FIXTURES"
    - from: "web/src/services/sqs/__tests__/msw-handlers.ts"
      to: "web/src/test/fixtures/sqs.ts"
      via: "import { SQS_FIXTURES }"
      pattern: "SQS_FIXTURES"
---

<objective>
Create Wave 0 test scaffolding for Phase 5: MSW handlers covering DDB (AWS-JSON via X-Amz-Target), SQS (AWS-JSON per D-10), IAM/STS (aws-query XML — STS as singleton per D-09), Secrets Manager / SSM / KMS (AWS-JSON with service-specific targets); JSON + XML fixtures from RESEARCH §9; and `test.todo()` stub files for every module named in RESEARCH §8 File Inventory (`_generic/`, `ddb/`, `sqs/`, plus 7 descriptor files — IAM split into 3 per D-09).

Purpose: Every later plan's `<verify>` command runs vitest against one of these stubs, so all must compile before production code begins. Locks fixture shapes so all downstream tests speak the same protocol. Satisfies Nyquist compliance — every requirement (DDB-01..03, SQS-01..03, GEN-01..03) gets a concrete automated command pointing at a real file.

Output: 7 fixture files, 3 MSW handler collections, 30+ test stub files containing ~80+ `test.todo()` entries that compile under strict TypeScript.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-dynamodb-sqs-generic/05-CONTEXT.md
@.planning/phases/05-dynamodb-sqs-generic/05-RESEARCH.md
@.planning/phases/04-lambda-service/04-00-PLAN.md
@web/src/services/lambda/__tests__/fixtures.ts
@web/src/services/lambda/__tests__/msw-handlers.ts

<interfaces>
<!-- Phase 4 established the Wave 0 pattern. Phase 5 mirrors but fans out across protocols. -->

Phase 4 fixtures pattern (flat JSON-only):
  export const LAMBDA_FIXTURES = { listFunctionsEmpty: { Functions: [] }, ... }

Phase 5 fixtures (this plan):
  - Multi-service split into 7 files under web/src/test/fixtures/
  - DDB / SQS / KMS / Secrets / SSM: JSON objects
  - IAM / STS: XML strings (aws-query protocol per §4.1–4.2)

MSW dispatch for AWS-JSON services (DDB / SQS / KMS / Secrets / SSM):
  The backend dispatches by the X-Amz-Target HEADER (not the URL path — URL is always "/").
  MSW handlers MUST read request.headers.get('x-amz-target') and switch to the right fixture.

MSW dispatch for aws-query services (IAM / STS):
  Same URL "/", but body is form-encoded. MSW handler reads the body string and
  parses the Action= parameter via URLSearchParams(await request.text()).

X-Amz-Target prefixes per RESEARCH §4:
  DynamoDB     — 'DynamoDB_20120810.{Action}'   (e.g. ListTables, DescribeTable, Scan, PutItem, ...)
  SQS (D-10)   — 'AmazonSQS.{Action}'            (e.g. ListQueues, GetQueueAttributes, ReceiveMessage, ...)
  KMS          — 'TrentService.{Action}'         (NOT 'KMS.' — historical; kms.py line 805)
  SecretsMgr   — 'secretsmanager.{Action}'       (lowercase prefix; secretsmanager.py line 128)
  SSM          — 'AmazonSSM.{Action}'            (ssm.py line 72)

STS singleton (D-05 + §5.2):
  GetCallerIdentity returns ONE record. No list endpoint. Descriptor kind='singleton'.
  MSW handler accepts Action=GetCallerIdentity and returns STS_FIXTURES.getCallerIdentityXml.

DDB Scan mixed-type fixture (RESEARCH §9.3):
  {"Items":[
   {"pk":{"S":"c-1"},"sk":{"S":"2026-04-01"},"total":{"N":"42"},"paid":{"BOOL":true}},
   {"pk":{"S":"c-1"},"sk":{"S":"2026-04-02"},"total":{"N":"7"},"paid":{"BOOL":false}},
   {"pk":{"S":"c-2"},"sk":{"S":"2026-04-01"},"total":{"N":"19"},"paid":{"NULL":true}}
  ],"Count":3,"ScannedCount":3}
  Note: N is a string per Pitfall 7.2.1; NULL is {NULL:true}; complex types (L,M,SS,NS,BS) are
  explicitly OUT OF SCOPE per D-06 and must NOT appear in Phase 5 fixtures.

File Inventory (RESEARCH §8.1 / §8.2 / §8.3 / §8.4) — this plan creates test.todo stubs for:
  _generic/ (15 source files → 15 tests): types, registry, keys, adapters×4, hooks×3,
            components×6 (Router/ListPage/DetailPanel/CreateModal/DeleteModal/DiffPreviewModal),
            columnBuilders, xmlUtils
  ddb/api/  (13 files): ddbClient, attributeValue, ddbKeys, useTables, useTable, useScan,
            useQuery, useItem, useCreateTable, useDeleteTable, usePutItem, useUpdateItem,
            useDeleteItem
  sqs/api/  (11 files): sqsClient, parseSqsAttributes, sqsKeys, useQueues, useQueueAttributes,
            useCreateQueue, useDeleteQueue, useSendMessage, useReceiveMessage,
            useDeleteMessage, usePurgeQueue
  descriptors (7 files per D-09): iam/descriptor.users, iam/descriptor.roles,
            iam/descriptor.policies, sts/descriptor, secretsmanager/descriptor,
            ssm/descriptor, kms/descriptor
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 7 fixture files and 3 MSW handler collections</name>
  <files>web/src/test/fixtures/ddb.ts, web/src/test/fixtures/sqs.ts, web/src/test/fixtures/iam.ts, web/src/test/fixtures/sts.ts, web/src/test/fixtures/kms.ts, web/src/test/fixtures/secretsmanager.ts, web/src/test/fixtures/ssm.ts, web/src/services/ddb/__tests__/msw-handlers.ts, web/src/services/sqs/__tests__/msw-handlers.ts, web/src/services/_generic/__tests__/msw-handlers.ts</files>
  <read_first>
    - web/src/services/lambda/__tests__/fixtures.ts (Phase 4 pattern — mirror the export style)
    - web/src/services/lambda/__tests__/msw-handlers.ts (MSW v2 pattern)
    - .planning/phases/05-dynamodb-sqs-generic/05-RESEARCH.md §9 (lines 1163–1291) — all fixtures verbatim
    - .planning/phases/05-dynamodb-sqs-generic/05-RESEARCH.md §1 (DDB endpoints + X-Amz-Target values)
    - .planning/phases/05-dynamodb-sqs-generic/05-RESEARCH.md §2 (SQS endpoints, D-10 JSON wire)
    - .planning/phases/05-dynamodb-sqs-generic/05-RESEARCH.md §4 (IAM/STS/Secrets/SSM/KMS targets)
    - .planning/phases/05-dynamodb-sqs-generic/05-RESEARCH.md §7.2 pitfalls 1, 4, 7
  </read_first>
  <action>
    **web/src/test/fixtures/ddb.ts** — `export const DDB_FIXTURES = { ... }` with keys:
    - `listTables` — `{ TableNames: ['orders', 'users'] }` (RESEARCH §9.1)
    - `listTablesEmpty` — `{ TableNames: [] }`
    - `describeTableOrders` — verbatim RESEARCH §9.2: Table with KeySchema [pk HASH, sk RANGE], AttributeDefinitions [pk S, sk S], TableStatus ACTIVE, BillingModeSummary { BillingMode: 'PAY_PER_REQUEST' }
    - `describeTableHashOnly` — variant with only HASH key (KeySchema length 1)
    - `scanMixed` — verbatim RESEARCH §9.3: 3 items with S, N (string-encoded '42'/'7'/'19'), BOOL (true + false), NULL ({NULL:true}). Count:3, ScannedCount:3. No LastEvaluatedKey.
    - `scanPaginated` — same 3 items PLUS `LastEvaluatedKey: { pk: {S: 'c-2'}, sk: {S: '2026-04-01'} }` (Pitfall 7.2.2 — map, not string)
    - `scanEmpty` — `{ Items: [], Count: 0, ScannedCount: 0 }`
    - `queryResult` — `{ Items: [ { pk:{S:'c-1'}, sk:{S:'2026-04-01'}, total:{N:'42'} } ], Count: 1, ScannedCount: 1 }`
    - `getItem` — `{ Item: { pk:{S:'c-1'}, sk:{S:'2026-04-01'}, total:{N:'42'}, paid:{BOOL:true} } }`
    - `getItemMissing` — `{}` (no Item key per §1.7)
    - `putItemNone` — `{}` (ReturnValues=NONE per §9.4)
    - `updateItemAllNew` — `{ Attributes: { pk:{S:'c-1'}, sk:{S:'2026-04-01'}, total:{N:'99'} } }`
    - `deleteItem` — `{}`
    - `createTableResponse` — `{ TableDescription: describeTableOrders.Table with TableStatus: 'CREATING' }`
    - `deleteTableResponse` — `{ TableDescription: describeTableOrders.Table with TableStatus: 'DELETING' }`

    **web/src/test/fixtures/sqs.ts** — `export const SQS_FIXTURES = { ... }` with keys:
    - `listQueues` — verbatim §9.5: `{ QueueUrls: ['http://localhost:4566/000000000000/dev-orders', 'http://localhost:4566/000000000000/dev-events'] }`
    - `listQueuesEmpty` — `{ QueueUrls: [] }` (backend returns no QueueUrls key when empty; emulate both shapes for robustness — include empty array variant)
    - `getQueueAttributes` — verbatim §9.6 (all counts as strings per §2.3)
    - `getQueueUrl` — `{ QueueUrl: 'http://localhost:4566/000000000000/dev-orders' }`
    - `createQueue` — `{ QueueUrl: 'http://localhost:4566/000000000000/dev-new' }`
    - `sendMessage` — `{ MessageId: '11111111-1111-1111-1111-111111111111', MD5OfMessageBody: '5d41402abc4b2a76b9719d911017c592', MD5OfMessageAttributes: 'deadbeef' }`
    - `receiveMessagesOne` — verbatim §9.7
    - `receiveMessagesEmpty` — `{}` (no Messages key per §2.6 line 366)
    - `deleteMessage` — `{}`
    - `purgeQueue` — `{}`
    - `deleteQueue` — `{}`

    **web/src/test/fixtures/iam.ts** — `export const IAM_FIXTURES = { ... }` with XML string keys:
    - `listUsersXml` — verbatim §9.8 (2 members: alice, bob)
    - `listUsersEmptyXml` — same shape but `<Users></Users>`
    - `listRolesXml` — analogous with 1 member (RoleName 'ops-role', Arn `arn:aws:iam::000000000000:role/ops-role`)
    - `listPoliciesXml` — 1 member (PolicyName 'ReadOnly', Arn, AttachmentCount 3)
    - `getUserXml` — `<GetUserResponse><GetUserResult><User><UserName>alice</UserName><UserId>AIDAALICE00000000</UserId><Arn>arn:aws:iam::000000000000:user/alice</Arn><Path>/</Path><CreateDate>2026-04-10T00:00:00Z</CreateDate></User></GetUserResult></GetUserResponse>`
    All values are `string` (not objects). Embedded newlines OK.

    **web/src/test/fixtures/sts.ts** — `export const STS_FIXTURES = { getCallerIdentityXml: string }` — verbatim §9.9.

    **web/src/test/fixtures/kms.ts** — `export const KMS_FIXTURES = { ... }`:
    - `listKeys` — verbatim §9.10 (2 keys with opaque UUIDs)
    - `listKeysEmpty` — `{ Keys: [], Truncated: false }`
    - `describeKey` — `{ KeyMetadata: { KeyId: '44444444-4444-4444-4444-444444444444', Arn: 'arn:aws:kms:us-east-1:000000000000:key/44444444-...', CreationDate: 1713350000, Enabled: true, Description: '', KeyUsage: 'ENCRYPT_DECRYPT', KeyState: 'Enabled', Origin: 'AWS_KMS', KeyManager: 'CUSTOMER', KeySpec: 'SYMMETRIC_DEFAULT', EncryptionAlgorithms: ['SYMMETRIC_DEFAULT'], SigningAlgorithms: [] } }`

    **web/src/test/fixtures/secretsmanager.ts** — `export const SECRETS_FIXTURES = { ... }`:
    - `listSecrets` — verbatim §9.11
    - `listSecretsEmpty` — `{ SecretList: [] }`
    - `describeSecret` — same per-secret shape plus `VersionIdsToStages: { v1: ['AWSCURRENT'] }`

    **web/src/test/fixtures/ssm.ts** — `export const SSM_FIXTURES = { ... }`:
    - `describeParameters` — verbatim §9.12 (one SecureString param)
    - `describeParametersEmpty` — `{ Parameters: [] }`
    - `getParameterMasked` — `{ Parameter: { Name: '/app/db/password', Type: 'SecureString', Value: 'ENCRYPTED:YWJjZGVmMTIz', Version: 1, LastModifiedDate: 1713350000, ARN: '...', DataType: 'text' } }` (Pitfall 7.2.11 — ENCRYPTED prefix when WithDecryption:false)
    - `getParameterDecrypted` — same but `Value: 'actual-secret-value'` (WithDecryption:true)

    ---

    **web/src/services/ddb/__tests__/msw-handlers.ts** — Export `ddbHandlers: HttpHandler[]`:
    ```ts
    import { http, HttpResponse } from 'msw'
    import { DDB_FIXTURES } from '../../../test/fixtures/ddb'

    function ddbDispatch(target: string) {
      // Strip 'DynamoDB_20120810.' prefix → Action name. Switch over Action to fixture.
      const action = target.split('.').pop()
      switch (action) {
        case 'ListTables':    return DDB_FIXTURES.listTables
        case 'DescribeTable': return DDB_FIXTURES.describeTableOrders
        case 'Scan':          return DDB_FIXTURES.scanMixed
        case 'Query':         return DDB_FIXTURES.queryResult
        case 'GetItem':       return DDB_FIXTURES.getItem
        case 'PutItem':       return DDB_FIXTURES.putItemNone
        case 'UpdateItem':    return DDB_FIXTURES.updateItemAllNew
        case 'DeleteItem':    return DDB_FIXTURES.deleteItem
        case 'CreateTable':   return DDB_FIXTURES.createTableResponse
        case 'DeleteTable':   return DDB_FIXTURES.deleteTableResponse
        default: return null
      }
    }

    export const ddbHandlers = [
      http.post('*/', async ({ request }) => {
        const target = request.headers.get('x-amz-target') ?? ''
        if (!target.startsWith('DynamoDB_20120810.')) return  // let other handlers try
        const body = ddbDispatch(target)
        if (!body) return HttpResponse.json({ __type: 'UnknownOperation' }, { status: 400 })
        return HttpResponse.json(body)
      }),
    ]
    ```
    Returning `undefined` from an MSW handler lets the next handler match — this is how DDB/SQS/KMS/etc. coexist on the same URL.

    **web/src/services/sqs/__tests__/msw-handlers.ts** — Export `sqsHandlers` with the same pattern, target prefix `AmazonSQS.`, action→fixture map covering ListQueues, GetQueueAttributes, GetQueueUrl, CreateQueue, SendMessage, ReceiveMessage, DeleteMessage, PurgeQueue, DeleteQueue. (D-10: NEVER reads URLSearchParams for Action — always the header.)

    **web/src/services/_generic/__tests__/msw-handlers.ts** — Export `genericHandlers` dispatching across THREE protocols in a single handler function:
    1. `x-amz-target` starts with `TrentService.` → KMS_FIXTURES by action (ListKeys / DescribeKey)
    2. `x-amz-target` starts with `secretsmanager.` → SECRETS_FIXTURES (ListSecrets / DescribeSecret)
    3. `x-amz-target` starts with `AmazonSSM.` → SSM_FIXTURES (DescribeParameters / GetParameter — honor WithDecryption flag by peeking the body)
    4. Body (form-encoded, no X-Amz-Target) containing `Action=ListUsers`/`ListRoles`/`ListPolicies`/`GetUser` → IAM XML fixture (return `new HttpResponse(xml, { headers: { 'Content-Type': 'application/xml' } })`)
    5. Body containing `Action=GetCallerIdentity` → STS_FIXTURES.getCallerIdentityXml

    Parse the form body via `new URLSearchParams(await request.text())` per §2 "If the client ever needs the Query path".

    For all handlers: set response `Content-Type: application/x-amz-json-1.0` for JSON responses, `application/xml` for XML responses. Every `return HttpResponse.json(...)` already sets JSON content-type.

    Registry Safety: MSW v2 is already installed (Phase 1 via Lambda Wave 0). No npm install needed.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -cE "test/fixtures|services/(ddb|sqs|_generic)/__tests__/msw-handlers" ; test -f web/src/test/fixtures/ddb.ts && test -f web/src/test/fixtures/sqs.ts && test -f web/src/test/fixtures/iam.ts && test -f web/src/test/fixtures/sts.ts && test -f web/src/test/fixtures/kms.ts && test -f web/src/test/fixtures/secretsmanager.ts && test -f web/src/test/fixtures/ssm.ts && test -f web/src/services/ddb/__tests__/msw-handlers.ts && test -f web/src/services/sqs/__tests__/msw-handlers.ts && test -f web/src/services/_generic/__tests__/msw-handlers.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const DDB_FIXTURES" web/src/test/fixtures/ddb.ts` == 1
    - `grep -c "export const SQS_FIXTURES" web/src/test/fixtures/sqs.ts` == 1
    - `grep -c "export const IAM_FIXTURES" web/src/test/fixtures/iam.ts` == 1
    - `grep -c "export const STS_FIXTURES" web/src/test/fixtures/sts.ts` == 1
    - `grep -c "export const KMS_FIXTURES" web/src/test/fixtures/kms.ts` == 1
    - `grep -c "export const SECRETS_FIXTURES" web/src/test/fixtures/secretsmanager.ts` == 1
    - `grep -c "export const SSM_FIXTURES" web/src/test/fixtures/ssm.ts` == 1
    - `grep -cE "\"BOOL\":true|\"BOOL\": true" web/src/test/fixtures/ddb.ts` >= 1   (BOOL true path)
    - `grep -cE "\"BOOL\":false|\"BOOL\": false" web/src/test/fixtures/ddb.ts` >= 1   (BOOL false path)
    - `grep -cE "\"NULL\":true|\"NULL\": true" web/src/test/fixtures/ddb.ts` >= 1   (NULL type)
    - `grep -cE "\"N\":\"" web/src/test/fixtures/ddb.ts` >= 3   (Pitfall 7.2.1 — N stored as string)
    - `grep -c "LastEvaluatedKey" web/src/test/fixtures/ddb.ts` >= 1   (Pitfall 7.2.2 — map shape)
    - `grep -c "ENCRYPTED:" web/src/test/fixtures/ssm.ts` >= 1   (Pitfall 7.2.11 — SSM masked prefix)
    - `grep -c "GetCallerIdentity" web/src/test/fixtures/sts.ts` >= 1
    - `grep -c "<Users>" web/src/test/fixtures/iam.ts` >= 1   (XML shape, not JSON)
    - `grep -c "x-amz-target" web/src/services/ddb/__tests__/msw-handlers.ts` >= 1
    - `grep -c "DynamoDB_20120810" web/src/services/ddb/__tests__/msw-handlers.ts` >= 1
    - `grep -c "AmazonSQS" web/src/services/sqs/__tests__/msw-handlers.ts` >= 1   (D-10 — JSON protocol)
    - `grep -cE "URLSearchParams|form" web/src/services/sqs/__tests__/msw-handlers.ts` == 0   (D-10 — NOT form-encoded)
    - `grep -cE "TrentService|secretsmanager\\.|AmazonSSM" web/src/services/_generic/__tests__/msw-handlers.ts` >= 3
    - `grep -cE "Action=ListUsers|Action=GetCallerIdentity" web/src/services/_generic/__tests__/msw-handlers.ts` >= 2
    - `cd web && npx tsc --noEmit -p tsconfig.json` reports zero errors under `web/src/test/fixtures/**` and `web/src/services/**/__tests__/msw-handlers.ts`
  </acceptance_criteria>
  <done>7 fixture files exist with correct shapes (DDB scalars per D-06, XML for IAM/STS, ENCRYPTED prefix for SSM SecureString). 3 MSW handler files dispatch correctly by protocol (X-Amz-Target for aws-json, form-encoded Action= for aws-query). Files compile under strict TS. No new npm deps added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stub test files for every Phase 5 module (test.todo only, 80+ total)</name>
  <files>web/src/services/_generic/types.test.ts, web/src/services/_generic/registry.test.ts, web/src/services/_generic/keys.test.ts, web/src/services/_generic/adapters/awsJson.test.ts, web/src/services/_generic/adapters/awsQuery.test.ts, web/src/services/_generic/adapters/rest.test.ts, web/src/services/_generic/adapters/buildRequest.test.ts, web/src/services/_generic/hooks/useGenericList.test.ts, web/src/services/_generic/hooks/useGenericItem.test.ts, web/src/services/_generic/hooks/useGenericMutation.test.ts, web/src/services/_generic/GenericServiceRouter.test.tsx, web/src/services/_generic/GenericListPage.test.tsx, web/src/services/_generic/GenericDetailPanel.test.tsx, web/src/services/_generic/GenericCreateModal.test.tsx, web/src/services/_generic/GenericDeleteModal.test.tsx, web/src/services/_generic/GenericDiffPreviewModal.test.tsx, web/src/services/_generic/columnBuilders.test.ts, web/src/services/_generic/xmlUtils.test.ts, web/src/services/ddb/api/ddbClient.test.ts, web/src/services/ddb/api/attributeValue.test.ts, web/src/services/ddb/api/ddbKeys.test.ts, web/src/services/ddb/api/useTables.test.ts, web/src/services/ddb/api/useTable.test.ts, web/src/services/ddb/api/useScan.test.ts, web/src/services/ddb/api/useQuery.test.ts, web/src/services/ddb/api/useItem.test.ts, web/src/services/ddb/api/useCreateTable.test.ts, web/src/services/ddb/api/useDeleteTable.test.ts, web/src/services/ddb/api/usePutItem.test.ts, web/src/services/ddb/api/useUpdateItem.test.ts, web/src/services/ddb/api/useDeleteItem.test.ts, web/src/services/sqs/api/sqsClient.test.ts, web/src/services/sqs/api/parseSqsAttributes.test.ts, web/src/services/sqs/api/sqsKeys.test.ts, web/src/services/sqs/api/useQueues.test.ts, web/src/services/sqs/api/useQueueAttributes.test.ts, web/src/services/sqs/api/useCreateQueue.test.ts, web/src/services/sqs/api/useDeleteQueue.test.ts, web/src/services/sqs/api/useSendMessage.test.ts, web/src/services/sqs/api/useReceiveMessage.test.ts, web/src/services/sqs/api/useDeleteMessage.test.ts, web/src/services/sqs/api/usePurgeQueue.test.ts, web/src/services/iam/descriptor.users.test.ts, web/src/services/iam/descriptor.roles.test.ts, web/src/services/iam/descriptor.policies.test.ts, web/src/services/sts/descriptor.test.ts, web/src/services/secretsmanager/descriptor.test.ts, web/src/services/ssm/descriptor.test.ts, web/src/services/kms/descriptor.test.ts</files>
  <behavior>
    Each file contains a `describe('{moduleName}', () => { ... })` with at least 2 `test.todo()` entries naming the behavior to be implemented. Vitest reports them as todo (not passing, not failing).
  </behavior>
  <read_first>
    - web/src/services/lambda/api/lambdaClient.test.ts (Phase 4 stub template — mirror shape)
    - .planning/phases/05-dynamodb-sqs-generic/05-RESEARCH.md §7.2 pitfalls (todo labels must name the pitfalls they guard)
    - .planning/phases/05-dynamodb-sqs-generic/05-CONTEXT.md Decisions D-01..D-11 — every decision must map to ≥1 test.todo
    - .planning/phases/05-dynamodb-sqs-generic/05-RESEARCH.md §6 (TanStack key shapes)
  </read_first>
  <action>
    Every stub file imports `{ describe, test }` from `'vitest'` ONLY. No production imports (those modules don't exist yet). Required `test.todo()` labels per file (verbatim — greppable):

    **_generic/types.test.ts** [GEN-01/02/03 / D-01]:
      - `test.todo('Adapter discriminant union includes aws-json, aws-query, rest (§5.1)')`
      - `test.todo('ServiceDescriptor has kind: list | singleton discriminant (Pitfall 7.2.7 — STS singleton)')`
      - `test.todo('MutationSpec shape supports all three adapters (D-01)')`

    **_generic/registry.test.ts** [GEN-03 / D-05 / D-09]:
      - `test.todo('GENERIC_DESCRIPTORS has exactly 7 keys (D-05 — 3 IAM + STS + Secrets + SSM + KMS)')`
      - `test.todo('IAM descriptors registered under iam.users / iam.roles / iam.policies (D-09)')`
      - `test.todo('sts descriptor is marked kind: singleton (Pitfall 7.2.7)')`
      - `test.todo('adding a new descriptor makes it retrievable via GENERIC_DESCRIPTORS[key] (GEN-03 end-to-end)')`

    **_generic/keys.test.ts** [GEN-01]:
      - `test.todo('genericKeys.list(serviceKey) returns [generic, serviceKey, list]')`
      - `test.todo('genericKeys.item(serviceKey, id) returns [generic, serviceKey, item, id]')`

    **_generic/adapters/awsJson.test.ts** [GEN-01 / DDB-01 / Pitfall 7.1.2]:
      - `test.todo('POSTs to / with Content-Type: application/x-amz-json-1.0')`
      - `test.todo('attaches X-Amz-Target header from args.target verbatim')`
      - `test.todo('attaches Authorization header scoped to args.credentialScope (kms/secretsmanager/ssm)')`
      - `test.todo('returns parsed JSON response body')`
      - `test.todo('propagates non-2xx as thrown Error preserving backend __type')`

    **_generic/adapters/awsQuery.test.ts** [GEN-01]:
      - `test.todo('POSTs to / with Content-Type: application/x-www-form-urlencoded')`
      - `test.todo('body contains Action={action}&Version={version} URL-encoded')`
      - `test.todo('returns raw XML string (descriptor parses via DOMParser)')`
      - `test.todo('Authorization scope matches args.credentialScope (iam/sts)')`

    **_generic/adapters/rest.test.ts** [GEN-01]:
      - `test.todo('method/path/body passed through to fetch')`
      - `test.todo('searchParams URL-encoded into querystring')`

    **_generic/adapters/buildRequest.test.ts** [D-07 / Pitfall 7.2.6]:
      - `test.todo('buildRequest for aws-json returns { url, headers, body } without sending — pure (D-07 preview)')`
      - `test.todo('buildRequest for aws-query includes Action + Version in body')`
      - `test.todo('adapter.send(spec,input) and buildRequest(spec,input) produce byte-identical body (Pitfall 7.2.6 — preview never lies)')`

    **_generic/hooks/useGenericList.test.ts** [GEN-01]:
      - `test.todo('queryKey is genericKeys.list(serviceKey)')`
      - `test.todo('calls descriptor.list.endpoint adapter and passes raw to parseResponse')`
      - `test.todo('returns Row[] — downstream table consumes directly')`

    **_generic/hooks/useGenericItem.test.ts** [GEN-02]:
      - `test.todo('queryKey is genericKeys.item(serviceKey, id)')`
      - `test.todo('singleton descriptor (kind: singleton) queries detail with empty id (STS case)')`

    **_generic/hooks/useGenericMutation.test.ts** [D-02 / D-07]:
      - `test.todo('exposes preview() pure-function accessor returning buildRequest output (D-07)')`
      - `test.todo('send() fires the mutation and invalidates genericKeys.list(serviceKey) (Pitfall 7.1.3 — single invalidate)')`
      - `test.todo('missing mutations.create disables Create in UI (D-02 read-only mode)')`

    **_generic/GenericServiceRouter.test.tsx** [GEN-01 / Pitfall 7.1.1 / Pitfall 7.2.7]:
      - `test.todo('unknown serviceKey renders NotFound message')`
      - `test.todo('kind: list routes /services/:serviceKey to GenericListPage')`
      - `test.todo('kind: singleton routes /services/sts directly to GenericDetailPanel (Pitfall 7.2.7)')`
      - `test.todo('registered AFTER /services/dynamodb and /services/sqs in router (Pitfall 7.1.1)')`

    **_generic/GenericListPage.test.tsx** [GEN-01 / D-02]:
      - `test.todo('renders Cloudscape Table with descriptor.list.columns')`
      - `test.todo('empty state shows descriptor.list.emptyStateCopy')`
      - `test.todo('Create button visible iff descriptor.mutations.create present (D-02)')`
      - `test.todo('Delete row action visible iff descriptor.mutations.delete present (D-02)')`

    **_generic/GenericDetailPanel.test.tsx** [GEN-02]:
      - `test.todo('renders <pre>JSON.stringify(detail, null, 2)</pre>')`
      - `test.todo('CopyToClipboard button copies the serialized JSON')`

    **_generic/GenericCreateModal.test.tsx** [D-02 / D-07]:
      - `test.todo('renders one FormField per bodyShape.fields entry')`
      - `test.todo('kind: string → Input; kind: number → Input numeric; kind: boolean → Toggle; kind: json → Textarea')`
      - `test.todo('submit first opens GenericDiffPreviewModal (D-07) — does not immediately send')`

    **_generic/GenericDeleteModal.test.tsx** [D-02 / D-07]:
      - `test.todo('type-to-confirm field matches descriptor.mutations.delete.typeToConfirmField')`
      - `test.todo('clicking Delete shows JSON diff preview (D-07) before firing mutation')`

    **_generic/GenericDiffPreviewModal.test.tsx** [D-07 / Pitfall 7.2.6]:
      - `test.todo('renders formatted JSON of buildRequest output (url/headers/body)')`
      - `test.todo('Send click invokes adapter with same object — no rebuild (Pitfall 7.2.6)')`
      - `test.todo('Cancel closes without firing the mutation')`

    **_generic/columnBuilders.test.ts** [GEN-01]:
      - `test.todo('arnColumn truncates middle of long ARN with ellipsis and adds copy button')`
      - `test.todo('timestampColumn uses RelativeTime component')`

    **_generic/xmlUtils.test.ts** [D-09 / §5.6]:
      - `test.todo('parseMembers walks <Root><Collection><member>...</member></Collection></Root>')`
      - `test.todo('parseMembers returns [] when collection is empty')`
      - `test.todo('parseMembers preserves field order declared in fields argument')`

    **ddb/api/ddbClient.test.ts** [DDB-01/02/03 / D-10 parallel]:
      - `test.todo('ddbJsonCall POSTs to / with X-Amz-Target: DynamoDB_20120810.{action}')`
      - `test.todo('Content-Type: application/x-amz-json-1.0')`
      - `test.todo('Authorization scope is dynamodb')`
      - `test.todo('returns parsed JSON body')`

    **ddb/api/attributeValue.test.ts** [DDB-02/03 / D-06 / Pitfall 7.2.1]:
      - `test.todo('marshalScalar for S returns {S: string}')`
      - `test.todo('marshalScalar for N stringifies number (Pitfall 7.2.1 — N is always a string on the wire)')`
      - `test.todo('marshalScalar for BOOL returns {BOOL: boolean}')`
      - `test.todo('marshalScalar for NULL returns {NULL: true}')`
      - `test.todo('marshalScalar for B accepts pre-encoded base64 string')`
      - `test.todo('renderAttributeValue flattens S/N/BOOL/NULL/B to display string')`
      - `test.todo('marshalScalar throws for complex types L/M/SS/NS/BS (D-06 — JSON-mode only)')`

    **ddb/api/ddbKeys.test.ts** [DDB-01/02/03]:
      - `test.todo('ddbKeys.tables() returns [ddb, tables]')`
      - `test.todo('ddbKeys.table(name) returns [ddb, table, name]')`
      - `test.todo('ddbKeys.scan encodes filter + ESK into key (Pitfall 7.2.2 — ESK is JSON-stringified)')`
      - `test.todo('ddbKeys.query encodes pkValue + ESK into key')`
      - `test.todo('ddbKeys.item(name, pkJson) returns [ddb, item, name, pkJson]')`

    **ddb/api/useTables.test.ts** [DDB-01]:
      - `test.todo('parses TableNames array from ListTables response')`
      - `test.todo('returns empty list when TableNames is undefined')`

    **ddb/api/useTable.test.ts** [DDB-01]:
      - `test.todo('parses DescribeTable into KeySchema / AttributeDefinitions / BillingMode')`
      - `test.todo('exposes hashKey and optional sortKey derived from KeySchema')`

    **ddb/api/useScan.test.ts** [DDB-02 / Pitfall 7.2.2]:
      - `test.todo('sends Scan with TableName and optional FilterExpression')`
      - `test.todo('LastEvaluatedKey round-trip — stringified for key, parsed for ExclusiveStartKey (Pitfall 7.2.2)')`
      - `test.todo('mixed-type items (S,N,BOOL,NULL) parse without loss')`

    **ddb/api/useQuery.test.ts** [DDB-02]:
      - `test.todo('sends KeyConditionExpression with pk value')`
      - `test.todo('base-table only — no IndexName (CONTEXT deferred)')`

    **ddb/api/useItem.test.ts** [DDB-02]:
      - `test.todo('sends GetItem with Key map built from AttributeValues')`
      - `test.todo('missing item returns undefined (no Item key in response — §1.7)')`

    **ddb/api/useCreateTable.test.ts** [DDB-01]:
      - `test.todo('supports hash-only KeySchema')`
      - `test.todo('supports hash+range KeySchema')`
      - `test.todo('BillingMode defaults to PAY_PER_REQUEST')`
      - `test.todo('invalidates ddbKeys.tables() on success (Pitfall 7.1.3)')`

    **ddb/api/useDeleteTable.test.ts** [DDB-01]:
      - `test.todo('sends DeleteTable with TableName')`
      - `test.todo('invalidates ddbKeys.tables() and ddbKeys.table(name)')`

    **ddb/api/usePutItem.test.ts** [DDB-03]:
      - `test.todo('marshals Item from native values to AttributeValue map')`
      - `test.todo('invalidates ddbKeys.scan prefix (not the whole ddb tree — Pitfall 7.1.3)')`

    **ddb/api/useUpdateItem.test.ts** [DDB-03]:
      - `test.todo('builds SET-only UpdateExpression (no REMOVE/ADD/DELETE per §1.9)')`
      - `test.todo('maps ExpressionAttributeNames / Values from dirty fields')`
      - `test.todo('ReturnValues: ALL_NEW used so UI can refresh item inline')`

    **ddb/api/useDeleteItem.test.ts** [DDB-03]:
      - `test.todo('sends Key map built from PK (+ optional SK)')`
      - `test.todo('invalidates scan prefix + item exact key')`

    **sqs/api/sqsClient.test.ts** [SQS-01/02/03 / D-10]:
      - `test.todo('sqsJsonCall POSTs to / with X-Amz-Target: AmazonSQS.{action} (D-10 — JSON not Query)')`
      - `test.todo('Content-Type: application/x-amz-json-1.0 (D-10)')`
      - `test.todo('body is JSON.stringify — NOT URLSearchParams (D-10, Pitfall 7.2.3 avoidance)')`
      - `test.todo('Authorization scope is sqs')`

    **sqs/api/parseSqsAttributes.test.ts** [SQS-01]:
      - `test.todo('parses ApproximateNumberOfMessages from string to number for display (§2.3 — all values are strings)')`
      - `test.todo('preserves unknown attribute keys as raw strings')`
      - `test.todo('coerces CreatedTimestamp string to Date')`

    **sqs/api/sqsKeys.test.ts** [SQS-01]:
      - `test.todo('sqsKeys.queues(prefix?) returns [sqs, queues, prefix ?? null]')`
      - `test.todo('sqsKeys.attributes(url) returns [sqs, attributes, url]')`

    **sqs/api/useQueues.test.ts** [SQS-01 / Pitfall 7.2.12]:
      - `test.todo('parses QueueUrls from ListQueues response')`
      - `test.todo('fans out GetQueueAttributes via useQueries (Pitfall 7.2.12 — parallel, not serial)')`

    **sqs/api/useQueueAttributes.test.ts** [SQS-01]:
      - `test.todo('requests AttributeNames: [All]')`
      - `test.todo('returns parsed counts as numbers')`

    **sqs/api/useCreateQueue.test.ts** [SQS-01]:
      - `test.todo('sends QueueName + optional Attributes map')`
      - `test.todo('invalidates sqsKeys.queues() on success')`

    **sqs/api/useDeleteQueue.test.ts** [SQS-01]:
      - `test.todo('sends QueueUrl')`
      - `test.todo('invalidates sqsKeys.queues()')`

    **sqs/api/useSendMessage.test.ts** [SQS-02]:
      - `test.todo('sends QueueUrl + MessageBody as plain JSON object (D-10)')`
      - `test.todo('MessageAttributes is plain object (not flattened MessageAttribute.1.Name — Pitfall 7.2.3 avoidance)')`
      - `test.todo('invalidates sqsKeys.attributes(url) to refresh counts')`

    **sqs/api/useReceiveMessage.test.ts** [SQS-02 / D-04]:
      - `test.todo('sends WaitTimeSeconds: 0 by default (D-04 — manual poll, no long-poll)')`
      - `test.todo('sends MaxNumberOfMessages: 10 by default (D-04)')`
      - `test.todo('empty response ({} — no Messages key) returns [] (§2.6)')`
      - `test.todo('successive polls APPEND to Zustand messageStore (D-04 — accumulating list)')`

    **sqs/api/useDeleteMessage.test.ts** [SQS-02 / Pitfall 7.2.4]:
      - `test.todo('sends QueueUrl + ReceiptHandle in JSON body (no URL encoding needed in body — Pitfall 7.2.4)')`
      - `test.todo('removes the deleted message from Zustand messageStore')`
      - `test.todo('invalidates sqsKeys.attributes(url)')`

    **sqs/api/usePurgeQueue.test.ts** [SQS-03 / Pitfall 7.2.4]:
      - `test.todo('sends PurgeQueue with QueueUrl')`
      - `test.todo('clears Zustand messageStore[url]')`
      - `test.todo('invalidates sqsKeys.attributes(url) only (Pitfall 7.1.3)')`
      - `test.todo('no client-side 60s cooldown (Pitfall 7.2.4 — MiniStack has no cooldown; real AWS does — documented in copy but not enforced)')`

    **iam/descriptor.users.test.ts** [GEN-01/02/03 / D-09]:
      - `test.todo('serviceKey === iam.users (D-09)')`
      - `test.todo('list.parseResponse walks ListUsersResult > Users > member from XML')`
      - `test.todo('detail.parseResponse reads GetUserResult > User fields')`
      - `test.todo('mutations.create has UserName + optional Path fields')`
      - `test.todo('mutations.delete.typeToConfirmField === UserName')`

    **iam/descriptor.roles.test.ts** [GEN-01/02/03 / D-09]:
      - `test.todo('serviceKey === iam.roles (D-09)')`
      - `test.todo('list.parseResponse walks ListRolesResult > Roles > member')`
      - `test.todo('mutations.delete.typeToConfirmField === RoleName')`

    **iam/descriptor.policies.test.ts** [GEN-01/02/03 / D-09]:
      - `test.todo('serviceKey === iam.policies (D-09)')`
      - `test.todo('list.parseResponse surfaces PolicyName, Arn, AttachmentCount')`

    **sts/descriptor.test.ts** [GEN-02 / Pitfall 7.2.7]:
      - `test.todo('serviceKey === sts')`
      - `test.todo('kind === singleton (Pitfall 7.2.7)')`
      - `test.todo('no list endpoint — only detail')`
      - `test.todo('detail.parseResponse returns { Arn, UserId, Account }')`

    **secretsmanager/descriptor.test.ts** [GEN-01/02 / D-08 / Pitfall 7.2.10]:
      - `test.todo('serviceKey === secretsmanager')`
      - `test.todo('list.endpoint.target === secretsmanager.ListSecrets (lowercase prefix)')`
      - `test.todo('detail uses DescribeSecret (metadata only) — NOT GetSecretValue by default (Pitfall 7.2.10)')`
      - `test.todo('no mutations (Phase 5 read-only for secrets)')`
      - `test.todo('value display is masked by default per D-08')`

    **ssm/descriptor.test.ts** [GEN-01/02 / D-08 / Pitfall 7.2.11]:
      - `test.todo('serviceKey === ssm')`
      - `test.todo('list.endpoint.target === AmazonSSM.DescribeParameters')`
      - `test.todo('detail endpoint sends WithDecryption: false by default (Pitfall 7.2.11)')`
      - `test.todo('SecureString values render masked per D-08')`

    **kms/descriptor.test.ts** [GEN-01/02 / Pitfall 7.2.8]:
      - `test.todo('serviceKey === kms')`
      - `test.todo('list.endpoint.target === TrentService.ListKeys (historical prefix, not KMS.*)')`
      - `test.todo('detail.endpoint.target === TrentService.DescribeKey')`
      - `test.todo('no mutations (D-02 — read-only)')`
      - `test.todo('KeyId treated as opaque string — no validation (Pitfall 7.2.8)')`

    Each stub imports only `{ describe, test }` from `'vitest'`. No production imports.
  </action>
  <verify>
    <automated>cd web && npm run test -- src/services/_generic src/services/ddb src/services/sqs src/services/iam src/services/sts src/services/kms src/services/secretsmanager src/services/ssm --run 2>&1 | grep -E "todo|Tests.*(passed|skipped)"</automated>
  </verify>
  <acceptance_criteria>
    - `find web/src/services/_generic web/src/services/ddb web/src/services/sqs web/src/services/iam web/src/services/sts web/src/services/kms web/src/services/secretsmanager web/src/services/ssm -name "*.test.*" 2>/dev/null | wc -l` >= 49
    - `grep -rc "test\.todo" web/src/services/_generic web/src/services/ddb web/src/services/sqs web/src/services/iam web/src/services/sts web/src/services/kms web/src/services/secretsmanager web/src/services/ssm 2>/dev/null | awk -F: '{s+=$2} END {print s}'` >= 80
    - Zero `expect(` calls in any stub: `grep -rc "expect(" web/src/services/_generic web/src/services/ddb web/src/services/sqs web/src/services/iam web/src/services/sts web/src/services/kms web/src/services/secretsmanager web/src/services/ssm 2>/dev/null | awk -F: '{s+=$2} END {print s}'` == 0
    - Every decision hook: `grep -rcE "D-0[1-9]|D-10|D-11" web/src/services/_generic web/src/services/ddb web/src/services/sqs web/src/services/iam web/src/services/sts web/src/services/kms web/src/services/secretsmanager web/src/services/ssm 2>/dev/null | awk -F: '{s+=$2} END {print s}'` >= 15
    - Pitfall hooks: `grep -rcE "Pitfall 7\.[12]\.[0-9]+" web/src/services/_generic web/src/services/ddb web/src/services/sqs web/src/services/iam web/src/services/sts web/src/services/kms web/src/services/secretsmanager web/src/services/ssm 2>/dev/null | awk -F: '{s+=$2} END {print s}'` >= 15
    - `cd web && npm run test -- src/services/_generic src/services/ddb src/services/sqs src/services/iam src/services/sts src/services/kms src/services/secretsmanager src/services/ssm --run` exits 0 with all results marked todo
    - `cd web && npx tsc --noEmit -p tsconfig.json` zero errors
  </acceptance_criteria>
  <done>49 stub files exist across 8 service roots, contain ≥80 total test.todo entries, compile under strict TS, vitest reports all as todo. Every locked decision D-01..D-11 and every named Phase-5 pitfall is hooked by ≥1 todo label.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Wave 0 creates test-only infrastructure. No runtime trust boundaries crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-00-01 | Tampering | MSW fixtures in test bundle | accept | Test-only code excluded from production by vitest/vite. No user-facing surface. |
| T-5-00-02 | Information Disclosure | secret-shaped fixtures (SSM SecureString, Secrets Manager) | accept | Synthetic values only (`ENCRYPTED:YWJjZGVmMTIz`, `db-creds`). No real credentials. |
| T-5-00-03 | Tampering | handler dispatch by user-controllable X-Amz-Target | accept | MSW handlers in tests only; backend dispatch is the same pattern and is already security-reviewed. |
</threat_model>

<deviation_framework>
If the executor encounters friction implementing this plan:

- **Rule 1 (auto-fix trivia):** Typos in fixture keys, import path casing, missing semicolons, obvious TS strict-mode errors — fix silently and note under "Rule 1 fixups" in SUMMARY.
- **Rule 2 (document deviations):** Additional fixture keys needed by downstream plans (e.g. a third DDB table variant), MSW matcher tweaks for test-isolation, different singleton XML root names observed in backend — implement, document under "Rule 2 deviations" with one-line rationale.
- **Rule 3 (blocker — stop + ask):** Any MSW handler that cannot faithfully reproduce the backend's X-Amz-Target dispatch (e.g. MSW v2 cannot read headers reliably in jsdom); any fixture whose shape disagrees with the backend handler cited in RESEARCH §1/§2/§4; STS requiring a list endpoint after all; D-10 (SQS JSON) not actually supported by the backend for some action. Stop, open the blocker in SUMMARY, surface to human.
</deviation_framework>

<verification>
Running `cd web && npm run test -- src/services/_generic src/services/ddb src/services/sqs src/services/iam src/services/sts src/services/kms src/services/secretsmanager src/services/ssm --run` exits 0 and reports all tests as todo. Running `cd web && npx tsc --noEmit -p tsconfig.json` reports zero errors.

Registry Safety grep — MUST return no new dependencies:
  `git diff HEAD web/package.json web/package-lock.json | grep -cE "^\\+.*\"[a-z@]" || echo 0`
  Expected: 0. If non-zero → Rule 3 blocker.
</verification>

<success_criteria>
7 fixture files + 3 MSW handler collections + 49 test stub files exist. Every downstream plan can reference a concrete test file in its `<verify>` block. Vitest infrastructure green before production code is written. Every CONTEXT decision D-01..D-11 and every named RESEARCH §7.2 pitfall is hooked by at least one todo label. Zero new npm dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dynamodb-sqs-generic/05-00-SUMMARY.md`
</output>
