---
phase: 02-ec2-dashboard-crud-patterns
plan: 06
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - web/src/services/ec2/api/elasticIps.ts
  - web/src/services/ec2/api/internetGateways.ts
  - web/src/services/ec2/api/natGateways.ts
  - web/src/services/ec2/pages/ElasticIpsTab.tsx
  - web/src/services/ec2/pages/InternetGatewaysTab.tsx
  - web/src/services/ec2/pages/NatGatewaysTab.tsx
  - web/src/app/routes.tsx
autonomous: true
requirements:
  - EC2-05
  - CRUD-01
  - CRUD-02
  - CRUD-03
  - CRUD-04
  - CRUD-06

must_haves:
  truths:
    - "User sees Elastic IP list with columns: Name, Allocation ID, Public IPv4, Associated instance, Private IPv4"
    - "User sees Internet Gateway list with columns: Name, IGW ID, State, VPC ID"
    - "User sees NAT Gateway list with columns: Name, NAT GW ID, State, Subnet ID, Public IP, VPC ID"
    - "User can allocate/release Elastic IPs, create/delete Internet Gateways and NAT Gateways"
    - "User can click row to see detail in SplitPanel"
  artifacts:
    - path: "web/src/services/ec2/api/elasticIps.ts"
      provides: "useElasticIps, useAllocateAddress, useReleaseAddress hooks"
    - path: "web/src/services/ec2/api/internetGateways.ts"
      provides: "useInternetGateways, useCreateInternetGateway, useDeleteInternetGateway hooks"
    - path: "web/src/services/ec2/api/natGateways.ts"
      provides: "useNatGateways, useCreateNatGateway, useDeleteNatGateway hooks"
    - path: "web/src/services/ec2/pages/ElasticIpsTab.tsx"
      provides: "Elastic IP management tab"
    - path: "web/src/services/ec2/pages/InternetGatewaysTab.tsx"
      provides: "Internet Gateway management tab"
    - path: "web/src/services/ec2/pages/NatGatewaysTab.tsx"
      provides: "NAT Gateway management tab"
  key_links:
    - from: "web/src/services/ec2/api/elasticIps.ts"
      to: "web/src/services/ec2/api/ec2Client.ts"
      via: "ec2Query"
      pattern: "ec2Query\\('DescribeAddresses'"
---

<objective>
Build Elastic IPs, Internet Gateways, and NAT Gateways tabs — three networking resource types with full CRUD.

Purpose: Fulfills EC2-05 (Elastic IP, NAT Gateway, Internet Gateway management).
Output: Three fully functional resource management tabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ec2-dashboard-crud-patterns/02-CONTEXT.md
@.planning/phases/02-ec2-dashboard-crud-patterns/02-RESEARCH.md
@.planning/phases/02-ec2-dashboard-crud-patterns/02-UI-SPEC.md
@.planning/phases/02-ec2-dashboard-crud-patterns/02-02-SUMMARY.md

<interfaces>
From Plan 01/02 outputs:
```typescript
export interface Ec2ElasticIp { allocationId, publicIp, instanceId, privateIpAddress, associationId, nameTag }
export interface Ec2InternetGateway { internetGatewayId, attachedVpcId, attachmentState, nameTag }
export interface Ec2NatGateway { natGatewayId, state, subnetId, vpcId, publicIp, nameTag }
// ec2Client, xml, ResourceTable, DeleteModal, CreateModal, SplitPanelDetail, FlashNotifications all available
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Elastic IPs, Internet Gateways, NAT Gateways API hooks and XML parsers</name>
  <files>
    web/src/services/ec2/api/elasticIps.ts,
    web/src/services/ec2/api/internetGateways.ts,
    web/src/services/ec2/api/natGateways.ts
  </files>
  <read_first>
    web/src/services/ec2/api/ec2Client.ts,
    web/src/services/ec2/api/types.ts,
    web/src/shared/api/xml.ts,
    .planning/phases/02-ec2-dashboard-crud-patterns/02-RESEARCH.md
  </read_first>
  <action>
1. Create `web/src/services/ec2/api/elasticIps.ts`:
   - `parseElasticIps(xml: string): Ec2ElasticIp[]` — parse DescribeAddresses XML. Addresses in `addressesSet > item`.
   - `useElasticIps()` — queryKey `['ec2', 'elastic-ips']`, staleTime 30_000
   - `useAllocateAddress()` — mutation: `ec2Query('AllocateAddress', { Domain: 'vpc' })`. Invalidate `['ec2', 'elastic-ips']`.
   - `useReleaseAddress()` — mutation: `ec2Query('ReleaseAddress', { AllocationId })`. Invalidate `['ec2', 'elastic-ips']`.

2. Create `web/src/services/ec2/api/internetGateways.ts`:
   - `parseInternetGateways(xml: string): Ec2InternetGateway[]` — parse DescribeInternetGateways XML. IGWs in `internetGatewaySet > item`. For attachedVpcId/attachmentState: check `attachmentSet > item`.
   - `useInternetGateways()` — queryKey `['ec2', 'internet-gateways']`, staleTime 30_000
   - `useCreateInternetGateway()` — mutation. Optional Name tag via CreateTags. Invalidate `['ec2', 'internet-gateways']`.
   - `useDeleteInternetGateway()` — mutation. Invalidate `['ec2', 'internet-gateways']`.

3. Create `web/src/services/ec2/api/natGateways.ts`:
   - `parseNatGateways(xml: string): Ec2NatGateway[]` — parse DescribeNatGateways XML. NAT GWs in `natGatewaySet > item`. For publicIp: check `natGatewayAddressSet > item`.
   - `useNatGateways()` — queryKey `['ec2', 'nat-gateways']`, staleTime 30_000
   - `useCreateNatGateway()` — mutation: `ec2Query('CreateNatGateway', { SubnetId, AllocationId })`. Invalidate `['ec2', 'nat-gateways']`.
   - `useDeleteNatGateway()` — mutation: `ec2Query('DeleteNatGateway', { NatGatewayId })`. Invalidate `['ec2', 'nat-gateways']`.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/minsu/workspace/ministack/web && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>All 9 hooks (3 query + 6 mutation) and 3 XML parsers for Elastic IPs, Internet Gateways, and NAT Gateways compile.</done>
</task>

<task type="auto">
  <name>Task 2: ElasticIpsTab, InternetGatewaysTab, NatGatewaysTab pages and route wiring</name>
  <files>
    web/src/services/ec2/pages/ElasticIpsTab.tsx,
    web/src/services/ec2/pages/InternetGatewaysTab.tsx,
    web/src/services/ec2/pages/NatGatewaysTab.tsx,
    web/src/app/routes.tsx
  </files>
  <read_first>
    web/src/services/ec2/api/elasticIps.ts,
    web/src/services/ec2/api/internetGateways.ts,
    web/src/services/ec2/api/natGateways.ts,
    web/src/services/ec2/api/subnets.ts,
    web/src/services/ec2/components/ResourceTable.tsx,
    web/src/services/ec2/components/CreateModal.tsx,
    web/src/services/ec2/components/DeleteModal.tsx,
    web/src/services/ec2/components/SplitPanelDetail.tsx,
    web/src/services/ec2/components/FlashNotifications.tsx,
    web/src/app/routes.tsx,
    .planning/phases/02-ec2-dashboard-crud-patterns/02-UI-SPEC.md
  </read_first>
  <action>
1. Create `web/src/services/ec2/pages/ElasticIpsTab.tsx` (default export):
   - Columns per UI-SPEC: Name, Allocation ID, Public IPv4, Associated instance, Private IPv4
   - Row click: SplitPanel flat
   - Allocate: single-click action (no modal form), calls useAllocateAddress
   - Release: type-to-confirm with allocationId

2. Create `web/src/services/ec2/pages/InternetGatewaysTab.tsx` (default export):
   - Columns per UI-SPEC: Name, IGW ID, State (StatusBadge), VPC ID
   - Row click: SplitPanel flat
   - Create IGW modal: Name tag (optional)
   - Delete: type-to-confirm

3. Create `web/src/services/ec2/pages/NatGatewaysTab.tsx` (default export):
   - Columns per UI-SPEC: Name, NAT GW ID, State (StatusBadge), Subnet ID, Public IP, VPC ID
   - Row click: SplitPanel flat
   - Create NAT GW modal: Name tag, Subnet (Select from useSubnets — import from subnets.ts), Elastic IP (Select from useElasticIps, filter to unassociated only)
   - Delete: type-to-confirm

4. Update `web/src/app/routes.tsx`: Replace placeholder imports for elastic-ips, internet-gateways, nat-gateways
  </action>
  <verify>
    <automated>cd /mnt/c/Users/minsu/workspace/ministack/web && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>Elastic IPs (single-click allocate), Internet Gateways, and NAT Gateways tabs render with full CRUD including create/delete modals, SplitPanel detail views, and Flashbar notifications.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Allocate/Release/Create/Delete mutations | Resource lifecycle actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | Delete IGW/NAT GW/EIP | mitigate | Type-to-confirm modal prevents accidental deletion (D-10). |
| T-02-16 | Denial of Service | Allocate many EIPs | accept | Local dev tool. No billing. No resource limits meaningful for emulator. |
</threat_model>

<verification>
1. `cd web && npx tsc --noEmit` exits 0
2. `cd web && npx vitest run` — all tests pass
3. Elastic IPs tab: allocate creates EIP instantly, release with confirm
4. Internet Gateways tab: create/delete work
5. NAT Gateways tab: create with subnet + EIP dropdowns, delete works
</verification>

<success_criteria>
- All three tabs render with correct columns per UI-SPEC
- Elastic IP allocation is single-click (no modal form)
- NAT Gateway creation populates subnet dropdown and filters EIPs to unassociated only
- All mutations invalidate correct query keys
- Phase 1 tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-ec2-dashboard-crud-patterns/02-06-SUMMARY.md`
</output>
