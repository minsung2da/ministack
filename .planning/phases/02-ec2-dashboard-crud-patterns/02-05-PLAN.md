---
phase: 02-ec2-dashboard-crud-patterns
plan: 05
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - web/src/services/ec2/api/keyPairs.ts
  - web/src/services/ec2/api/volumes.ts
  - web/src/services/ec2/api/snapshots.ts
  - web/src/services/ec2/pages/KeyPairsTab.tsx
  - web/src/services/ec2/pages/VolumesTab.tsx
  - web/src/services/ec2/pages/SnapshotsTab.tsx
  - web/src/app/routes.tsx
autonomous: true
requirements:
  - EC2-03
  - EC2-04
  - CRUD-01
  - CRUD-02
  - CRUD-03
  - CRUD-04
  - CRUD-06

must_haves:
  truths:
    - "User sees Key Pair list with columns: Name, Key pair ID, Type, Fingerprint"
    - "User sees EBS Volume list with columns: Name, Volume ID, State, Size, Type, AZ, Attached to"
    - "User sees Snapshot list with columns: Name, Snapshot ID, State, Volume ID, Size, Started"
    - "User can create/delete key pairs, volumes, snapshots via modal forms"
    - "User can click row to see detail in SplitPanel"
  artifacts:
    - path: "web/src/services/ec2/api/keyPairs.ts"
      provides: "useKeyPairs, useCreateKeyPair, useDeleteKeyPair hooks"
    - path: "web/src/services/ec2/api/volumes.ts"
      provides: "useVolumes, useCreateVolume, useDeleteVolume hooks"
    - path: "web/src/services/ec2/api/snapshots.ts"
      provides: "useSnapshots, useCreateSnapshot, useDeleteSnapshot hooks"
    - path: "web/src/services/ec2/pages/KeyPairsTab.tsx"
      provides: "Key Pair management tab"
    - path: "web/src/services/ec2/pages/VolumesTab.tsx"
      provides: "EBS Volume management tab"
    - path: "web/src/services/ec2/pages/SnapshotsTab.tsx"
      provides: "Snapshot management tab"
  key_links:
    - from: "web/src/services/ec2/api/volumes.ts"
      to: "web/src/services/ec2/api/ec2Client.ts"
      via: "ec2Query"
      pattern: "ec2Query\\('DescribeVolumes'"
---

<objective>
Build Key Pairs, EBS Volumes, and Snapshots tabs — three storage/compute resource types with full CRUD.

Purpose: Fulfills EC2-03 (key pairs) and EC2-04 (EBS volumes, snapshots list/detail/create/delete).
Output: Three fully functional resource management tabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ec2-dashboard-crud-patterns/02-CONTEXT.md
@.planning/phases/02-ec2-dashboard-crud-patterns/02-RESEARCH.md
@.planning/phases/02-ec2-dashboard-crud-patterns/02-UI-SPEC.md
@.planning/phases/02-ec2-dashboard-crud-patterns/02-01-SUMMARY.md

<interfaces>
From Plan 01 outputs (same as Plan 04):
```typescript
// ec2Client, xml, types, ResourceTable, DeleteModal, CreateModal, SplitPanelDetail,
// StatusBadge, FlashNotifications, useSplitPanel — all available
export interface Ec2KeyPair { keyPairId, keyName, keyType, keyFingerprint }
export interface Ec2Volume { volumeId, status, size: number, volumeType, availabilityZone, attachedInstanceId, nameTag, createTime }
export interface Ec2Snapshot { snapshotId, status, volumeId, volumeSize: number, startTime, description, nameTag }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Key Pairs, Volumes, Snapshots API hooks and XML parsers</name>
  <files>
    web/src/services/ec2/api/keyPairs.ts,
    web/src/services/ec2/api/volumes.ts,
    web/src/services/ec2/api/snapshots.ts
  </files>
  <read_first>
    web/src/services/ec2/api/ec2Client.ts,
    web/src/services/ec2/api/types.ts,
    web/src/shared/api/xml.ts,
    .planning/phases/02-ec2-dashboard-crud-patterns/02-RESEARCH.md
  </read_first>
  <action>
1. Create `web/src/services/ec2/api/keyPairs.ts`:
   - `parseKeyPairs(xml: string): Ec2KeyPair[]` — parse DescribeKeyPairs XML. Key pairs in `keySet > item`. Extract: keyPairId, keyName, keyType, keyFingerprint.
   - `useKeyPairs()` — queryKey `['ec2', 'key-pairs']`, staleTime 30_000
   - `useCreateKeyPair()` — mutation: `ec2Query('CreateKeyPair', { KeyName, KeyType })`. Invalidate `['ec2', 'key-pairs']`. Parse response for keyPairId, keyFingerprint, and optionally keyMaterial (private key — display once in modal or flash).
   - `useDeleteKeyPair()` — mutation: `ec2Query('DeleteKeyPair', { KeyPairId })`. Invalidate `['ec2', 'key-pairs']`.

2. Create `web/src/services/ec2/api/volumes.ts`:
   - `parseVolumes(xml: string): Ec2Volume[]` — parse DescribeVolumes XML. Volumes in `volumeSet > item`. Extract: volumeId, status, size (parseInt), volumeType, availabilityZone, nameTag via getNameTag, createTime. For attachedInstanceId: check `attachmentSet > item`, get first `instanceId` if exists.
   - `useVolumes()` — queryKey `['ec2', 'volumes']`, staleTime 30_000
   - `useCreateVolume()` — mutation: `ec2Query('CreateVolume', { AvailabilityZone, Size: String(size), VolumeType })`. Optional Name tag via CreateTags (same pattern as VPCs). Invalidate `['ec2', 'volumes']`.
   - `useDeleteVolume()` — mutation: `ec2Query('DeleteVolume', { VolumeId })`. Invalidate `['ec2', 'volumes']`.

3. Create `web/src/services/ec2/api/snapshots.ts`:
   - `parseSnapshots(xml: string): Ec2Snapshot[]` — parse DescribeSnapshots XML. Snapshots in `snapshotSet > item`. Extract: snapshotId, status, volumeId, volumeSize (parseInt), startTime, description, nameTag.
   - `useSnapshots()` — queryKey `['ec2', 'snapshots']`, staleTime 30_000
   - `useCreateSnapshot()` — mutation: `ec2Query('CreateSnapshot', { VolumeId, Description })`. Invalidate `['ec2', 'snapshots']`.
   - `useDeleteSnapshot()` — mutation: `ec2Query('DeleteSnapshot', { SnapshotId })`. Invalidate `['ec2', 'snapshots']`.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/minsu/workspace/ministack/web && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - web/src/services/ec2/api/keyPairs.ts contains `export function useKeyPairs(`
    - web/src/services/ec2/api/keyPairs.ts contains `export function useCreateKeyPair(`
    - web/src/services/ec2/api/keyPairs.ts contains `export function useDeleteKeyPair(`
    - web/src/services/ec2/api/volumes.ts contains `export function useVolumes(`
    - web/src/services/ec2/api/volumes.ts contains `export function useCreateVolume(`
    - web/src/services/ec2/api/volumes.ts contains `attachmentSet`
    - web/src/services/ec2/api/snapshots.ts contains `export function useSnapshots(`
    - web/src/services/ec2/api/snapshots.ts contains `export function useCreateSnapshot(`
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>All 9 hooks (3 query + 6 mutation) and 3 XML parsers for Key Pairs, Volumes, and Snapshots compile.</done>
</task>

<task type="auto">
  <name>Task 2: KeyPairsTab, VolumesTab, SnapshotsTab pages and route wiring</name>
  <files>
    web/src/services/ec2/pages/KeyPairsTab.tsx,
    web/src/services/ec2/pages/VolumesTab.tsx,
    web/src/services/ec2/pages/SnapshotsTab.tsx,
    web/src/app/routes.tsx
  </files>
  <read_first>
    web/src/services/ec2/api/keyPairs.ts,
    web/src/services/ec2/api/volumes.ts,
    web/src/services/ec2/api/snapshots.ts,
    web/src/services/ec2/components/ResourceTable.tsx,
    web/src/services/ec2/components/CreateModal.tsx,
    web/src/services/ec2/components/DeleteModal.tsx,
    web/src/services/ec2/components/SplitPanelDetail.tsx,
    web/src/services/ec2/components/FlashNotifications.tsx,
    web/src/app/routes.tsx,
    .planning/phases/02-ec2-dashboard-crud-patterns/02-UI-SPEC.md
  </read_first>
  <action>
1. Create `web/src/services/ec2/pages/KeyPairsTab.tsx` (default export):
   - Columns per UI-SPEC: Name (keyName), Key pair ID, Type (keyType), Fingerprint
   - FilteringProperties: keyName (:, !:), keyPairId (=, !=), keyType (=, !=)
   - Row click: SplitPanel flat — KeyValuePairs showing keyPairId, keyName, keyType, keyFingerprint
   - Create Key Pair modal: Name (required, max 255, alphanumeric + hyphens + underscores), Type (Select: rsa, ed25519). On submit: useCreateKeyPair. SPECIAL: If response contains keyMaterial (private key), show it in a read-only textarea inside a success alert with a "Copy" button. This is the only time the private key is available.
   - Delete: type-to-confirm, useDeleteKeyPair

2. Create `web/src/services/ec2/pages/VolumesTab.tsx` (default export):
   - Columns per UI-SPEC: Name, Volume ID, State (StatusBadge), Size (GiB), Volume type, AZ, Attached to (instanceId or '-')
   - FilteringProperties: volumeId, status (=, !=), volumeType (=, !=), availabilityZone (=, !=), nameTag (:, !:)
   - Row click: SplitPanel flat — KeyValuePairs showing volumeId, status, size, volumeType, availabilityZone, attachedInstanceId, createTime, nameTag
   - Create Volume modal per UI-SPEC: Name tag (optional), Volume type (Select: gp2, gp3, io1, io2, standard), Size in GiB (number input, required, 1-16384), AZ (Select: us-east-1a, us-east-1b, us-east-1c, us-east-1d). On submit: useCreateVolume.
   - Delete: type-to-confirm, useDeleteVolume

3. Create `web/src/services/ec2/pages/SnapshotsTab.tsx` (default export):
   - Columns per UI-SPEC: Name, Snapshot ID, State (StatusBadge), Volume ID, Size (GiB), Started
   - FilteringProperties: snapshotId, status (=, !=), volumeId (=, !=, :), nameTag (:, !:)
   - Row click: SplitPanel flat — KeyValuePairs showing snapshotId, status, volumeId, volumeSize, startTime, description, nameTag
   - Create Snapshot modal per UI-SPEC: Volume (Select, required, populated from useVolumes — show available volumes), Description (optional, max 255). On submit: useCreateSnapshot.
   - Delete: type-to-confirm, useDeleteSnapshot

4. Update `web/src/app/routes.tsx`:
   - Replace placeholder imports for key-pairs, volumes, snapshots paths with real lazy imports
  </action>
  <verify>
    <automated>cd /mnt/c/Users/minsu/workspace/ministack/web && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - web/src/services/ec2/pages/KeyPairsTab.tsx contains `useKeyPairs`
    - web/src/services/ec2/pages/KeyPairsTab.tsx contains `keyMaterial` or `private key`
    - web/src/services/ec2/pages/VolumesTab.tsx contains `useVolumes`
    - web/src/services/ec2/pages/VolumesTab.tsx contains `gp2`
    - web/src/services/ec2/pages/VolumesTab.tsx contains `16384` or `max`
    - web/src/services/ec2/pages/SnapshotsTab.tsx contains `useSnapshots`
    - web/src/services/ec2/pages/SnapshotsTab.tsx contains `useVolumes` or `useCreateSnapshot`
    - web/src/app/routes.tsx contains `KeyPairsTab`
    - web/src/app/routes.tsx contains `VolumesTab`
    - web/src/app/routes.tsx contains `SnapshotsTab`
    - tsc --noEmit exits 0
    - vitest run exits 0
  </acceptance_criteria>
  <done>Key Pairs, Volumes, and Snapshots tabs render with full CRUD including create modals with proper field validation, delete confirmation, and SplitPanel detail views.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form -> Create mutations | Volume size, key pair name sent to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Information Disclosure | Key pair private key (keyMaterial) | mitigate | Display keyMaterial in a one-time alert with copy button. Never persist to localStorage or state beyond the create response. User is warned this is the only time to see it. |
| T-02-14 | Tampering | Volume size input | mitigate | Client-side validation: integer 1-16384. Backend also validates range. |
</threat_model>

<verification>
1. `cd web && npx tsc --noEmit` exits 0
2. `cd web && npx vitest run` — all tests pass
3. Key Pairs tab: create key pair shows private key once, delete works
4. Volumes tab: create volume with size/type/AZ, status badge, delete works
5. Snapshots tab: create from volume dropdown, delete works
</verification>

<success_criteria>
- All three tabs render with correct columns per UI-SPEC
- Key Pair creation shows private key material in one-time alert
- Volume creation validates size 1-16384 and requires AZ selection
- Snapshot creation populates volume dropdown from API
- All mutations invalidate correct query keys
- Phase 1 tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-ec2-dashboard-crud-patterns/02-05-SUMMARY.md`
</output>
