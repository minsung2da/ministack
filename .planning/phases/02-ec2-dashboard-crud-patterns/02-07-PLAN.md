---
phase: 02-ec2-dashboard-crud-patterns
plan: 07
type: execute
wave: 3
depends_on: [02-02, 02-03, 02-04, 02-05, 02-06]
files_modified:
  - web/src/services/ec2/api/routeTables.ts
  - web/src/services/ec2/api/networkInterfaces.ts
  - web/src/services/ec2/pages/RouteTablesTab.tsx
  - web/src/services/ec2/pages/NetworkInterfacesTab.tsx
  - web/src/app/routes.tsx
autonomous: false
requirements:
  - EC2-03
  - CRUD-01
  - CRUD-02

must_haves:
  truths:
    - "User sees Route Table list with columns: Name, Route Table ID, VPC ID, Main, Routes count"
    - "User sees Network Interface list with columns: Name, ENI ID, Status, VPC ID, Subnet ID, Primary private IP"
    - "Route Tables and Network Interfaces are list-only (no create/delete buttons) per D-12"
    - "Both use TextFilter (not PropertyFilter) per UI-SPEC"
    - "User can click row to see detail in SplitPanel"
    - "All 12 EC2 tabs are fully functional with no placeholders"
    - "Full test suite passes (vitest + existing Phase 1 tests)"
  artifacts:
    - path: "web/src/services/ec2/api/routeTables.ts"
      provides: "useRouteTables hook + parseRouteTables"
    - path: "web/src/services/ec2/api/networkInterfaces.ts"
      provides: "useNetworkInterfaces hook + parseNetworkInterfaces"
    - path: "web/src/services/ec2/pages/RouteTablesTab.tsx"
      provides: "Route Tables list tab (read-only)"
    - path: "web/src/services/ec2/pages/NetworkInterfacesTab.tsx"
      provides: "Network Interfaces list tab (read-only)"
  key_links:
    - from: "web/src/services/ec2/api/routeTables.ts"
      to: "web/src/services/ec2/api/ec2Client.ts"
      via: "ec2Query"
      pattern: "ec2Query\\('DescribeRouteTables'"
---

<objective>
Build the final two list-only tabs (Route Tables, Network Interfaces) and run a full integration checkpoint to verify all 12 EC2 resource types are functional.

Purpose: Completes EC2-03 coverage for the remaining list-only resources and provides the human verification gate for the entire Phase 2.
Output: All 12 EC2 tabs fully operational; human-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ec2-dashboard-crud-patterns/02-CONTEXT.md
@.planning/phases/02-ec2-dashboard-crud-patterns/02-RESEARCH.md
@.planning/phases/02-ec2-dashboard-crud-patterns/02-UI-SPEC.md
@.planning/phases/02-ec2-dashboard-crud-patterns/02-01-SUMMARY.md

<interfaces>
From Plan 01 types.ts:
```typescript
export interface Ec2RouteTable { routeTableId, vpcId, isMain: boolean, routeCount: number, nameTag }
export interface Ec2NetworkInterface { networkInterfaceId, status, vpcId, subnetId, privateIpAddress, nameTag }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route Tables and Network Interfaces API + list-only tabs</name>
  <files>
    web/src/services/ec2/api/routeTables.ts,
    web/src/services/ec2/api/networkInterfaces.ts,
    web/src/services/ec2/pages/RouteTablesTab.tsx,
    web/src/services/ec2/pages/NetworkInterfacesTab.tsx,
    web/src/app/routes.tsx
  </files>
  <read_first>
    web/src/services/ec2/api/ec2Client.ts,
    web/src/services/ec2/api/types.ts,
    web/src/shared/api/xml.ts,
    web/src/services/ec2/components/ResourceTable.tsx,
    web/src/services/ec2/components/SplitPanelDetail.tsx,
    web/src/app/routes.tsx,
    .planning/phases/02-ec2-dashboard-crud-patterns/02-UI-SPEC.md
  </read_first>
  <action>
1. Create `web/src/services/ec2/api/routeTables.ts`:
   - `parseRouteTables(xml: string): Ec2RouteTable[]` — parse DescribeRouteTables XML. Route tables in `routeTableSet > item`. Extract: routeTableId, vpcId, nameTag. isMain: check `associationSet > item` for any where `main` text is `'true'`. routeCount: count items in `routeSet`.
   - `useRouteTables()` — queryKey `['ec2', 'route-tables']`, staleTime 30_000
   - NO create/delete mutations (D-12: list-only)

2. Create `web/src/services/ec2/api/networkInterfaces.ts`:
   - `parseNetworkInterfaces(xml: string): Ec2NetworkInterface[]` — parse DescribeNetworkInterfaces XML. ENIs in `networkInterfaceSet > item`. Extract: networkInterfaceId, status, vpcId, subnetId, privateIpAddress, nameTag.
   - `useNetworkInterfaces()` — queryKey `['ec2', 'network-interfaces']`, staleTime 30_000
   - NO create/delete mutations (D-12: list-only)

3. Create `web/src/services/ec2/pages/RouteTablesTab.tsx` (default export):
   - Columns per UI-SPEC: Name, Route Table ID, VPC ID, Main (boolean "Yes"/"No"), Routes (count)
   - Uses `useTextFilter: true` on ResourceTable (TextFilter instead of PropertyFilter per UI-SPEC filtering contract for list-only resources)
   - NO selection (selectionType: 'none' or omit — no checkboxes)
   - NO create/delete buttons in header. Only Refresh button.
   - Row click: SplitPanel flat — KeyValuePairs showing routeTableId, vpcId, isMain, routeCount, nameTag

4. Create `web/src/services/ec2/pages/NetworkInterfacesTab.tsx` (default export):
   - Columns per UI-SPEC: Name, Network Interface ID, Status (StatusBadge), VPC ID, Subnet ID, Primary private IP
   - Uses `useTextFilter: true` on ResourceTable
   - NO selection, NO create/delete buttons. Only Refresh.
   - Row click: SplitPanel flat — KeyValuePairs showing all attributes

5. Update `web/src/app/routes.tsx`:
   - Replace placeholder imports for route-tables, network-interfaces paths with real lazy imports
   - At this point, ALL 12 tab routes should use real components. Remove the Ec2TabPlaceholder import if no longer referenced. Delete `web/src/services/ec2/pages/Ec2TabPlaceholder.tsx` file.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/minsu/workspace/ministack/web && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - web/src/services/ec2/api/routeTables.ts contains `export function useRouteTables(`
    - web/src/services/ec2/api/routeTables.ts does NOT contain `useCreate` or `useDelete` (list-only)
    - web/src/services/ec2/api/networkInterfaces.ts contains `export function useNetworkInterfaces(`
    - web/src/services/ec2/api/networkInterfaces.ts does NOT contain `useCreate` or `useDelete`
    - web/src/services/ec2/pages/RouteTablesTab.tsx contains `useTextFilter` or `TextFilter`
    - web/src/services/ec2/pages/NetworkInterfacesTab.tsx contains `useTextFilter` or `TextFilter`
    - web/src/app/routes.tsx does NOT contain `Ec2TabPlaceholder`
    - web/src/app/routes.tsx contains `RouteTablesTab`
    - web/src/app/routes.tsx contains `NetworkInterfacesTab`
    - tsc --noEmit exits 0
    - vitest run exits 0
  </acceptance_criteria>
  <done>Route Tables and Network Interfaces tabs render as list-only (no CRUD buttons, TextFilter) and all 12 EC2 tab routes use real components.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete EC2 Dashboard</name>
  <files>N/A</files>
  <action>
    Human verifies the complete EC2 Dashboard with all 12 resource tabs.

    What was built:
    - Instances: sortable/filterable table, color-coded status, start/stop/terminate/reboot actions, 4-step launch wizard, SplitPanel detail with 5 tabs
    - VPCs, Subnets, Security Groups: full CRUD (list, create modal, delete modal, SplitPanel detail)
    - Key Pairs, EBS Volumes, Snapshots: full CRUD with specialized forms
    - Elastic IPs: single-click allocate, release with confirm
    - Internet Gateways, NAT Gateways: full CRUD
    - Route Tables, Network Interfaces: list-only with TextFilter

    Steps to verify (prerequisites: start MiniStack):

    1. Navigate to http://localhost:4566/_console/services/ec2 — verify 12 tabs visible, defaults to Instances tab, tab clicks change URL

    2. Instances tab: create instance via CLI (`aws --endpoint-url=http://localhost:4566 ec2 run-instances --image-id ami-12345 --instance-type t2.micro`), click Refresh, verify green "running" badge, click row for SplitPanel with 5 tabs, test Stop/Terminate actions with state-aware disabling

    3. Launch Wizard: click "Launch instance", walk through 4 steps with live VPC/Subnet/SG dropdowns, verify instance appears after launch

    4. VPCs tab: verify default VPC, create VPC with CIDR 10.0.0.0/16, delete with type-to-confirm

    5. Spot-check 2-3 other tabs (Subnets, Key Pairs, Elastic IPs) for table/create/delete/detail

    6. Route Tables tab: verify list-only (no create/delete buttons), default route table visible
  </action>
  <verify>Human visual and functional verification</verify>
  <acceptance_criteria>
    - All 12 EC2 tabs render without errors
    - Instance actions (start/stop/terminate/reboot) work correctly
    - Launch wizard creates an instance successfully
    - Create/delete modals work for VPCs, Subnets, Security Groups
    - SplitPanel opens on row click for all resource types
    - Route Tables and Network Interfaces have no create/delete buttons
  </acceptance_criteria>
  <done>Human approves the complete EC2 Dashboard UX. Type "approved" or describe issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | List-only resources, no mutation surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Information Disclosure | Route table/ENI detail view | accept | Local dev tool. All data is from localhost emulator. No sensitive data exposure concern. |
</threat_model>

<verification>
1. `cd web && npx tsc --noEmit` exits 0
2. `cd web && npx vitest run` — all tests pass
3. All 12 EC2 tabs functional (human checkpoint)
4. No Ec2TabPlaceholder references remaining
5. Vite build succeeds: `cd web && npx vite build`
</verification>

<success_criteria>
- Route Tables and Network Interfaces render as list-only (no CRUD, TextFilter)
- All 12 EC2 tabs work end-to-end
- No placeholder components remain
- Phase 1 + Phase 2 test suite passes
- Human approves the full EC2 Dashboard UX
</success_criteria>

<output>
After completion, create `.planning/phases/02-ec2-dashboard-crud-patterns/02-07-SUMMARY.md`
</output>
