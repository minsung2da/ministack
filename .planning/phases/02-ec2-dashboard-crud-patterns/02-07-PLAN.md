---
phase: 02-ec2-dashboard-crud-patterns
plan: 07
type: execute
wave: 3
depends_on: [02-02, 02-04, 02-05]
files_modified:
  - web/src/services/ec2/api/images.ts
  - web/src/services/ec2/api/instances.ts
  - web/src/services/ec2/pages/InstanceWizard.tsx
  - web/src/app/routes.tsx
autonomous: true
requirements:
  - CRUD-03
  - EC2-06

must_haves:
  truths:
    - "User can launch a new instance through a 4-step wizard"
    - "Step 1 lets user set Name and select Instance Type from a dropdown"
    - "Step 2 lets user select VPC (populated from API) and Subnet (filtered by VPC) from dropdowns"
    - "Step 3 lets user select Security Groups (Multiselect, filtered by VPC) and Key Pair from dropdowns"
    - "Step 4 shows a review summary of all selections"
    - "Launch instance button calls RunInstances and shows Flashbar success on completion"
    - "Cancel returns to Instances tab without creating anything"
  artifacts:
    - path: "web/src/services/ec2/pages/InstanceWizard.tsx"
      provides: "4-step instance creation wizard"
    - path: "web/src/services/ec2/api/images.ts"
      provides: "useImages hook for AMI list"
  key_links:
    - from: "web/src/services/ec2/pages/InstanceWizard.tsx"
      to: "web/src/services/ec2/api/ec2Client.ts"
      via: "ec2Query for RunInstances"
      pattern: "ec2Query\\('RunInstances'"
    - from: "web/src/services/ec2/pages/InstanceWizard.tsx"
      to: "web/src/services/ec2/api/vpcs.ts"
      via: "useVpcs hook (canonical, from Plan 04)"
      pattern: "import.*useVpcs.*from.*vpcs"
    - from: "web/src/services/ec2/pages/InstanceWizard.tsx"
      to: "web/src/services/ec2/api/subnets.ts"
      via: "useSubnets hook (canonical, from Plan 04)"
      pattern: "import.*useSubnets.*from.*subnets"
    - from: "web/src/services/ec2/pages/InstanceWizard.tsx"
      to: "web/src/services/ec2/api/securityGroups.ts"
      via: "useSecurityGroups hook (canonical, from Plan 04)"
      pattern: "import.*useSecurityGroups.*from.*securityGroups"
    - from: "web/src/services/ec2/pages/InstanceWizard.tsx"
      to: "web/src/services/ec2/api/keyPairs.ts"
      via: "useKeyPairs hook (canonical, from Plan 05)"
      pattern: "import.*useKeyPairs.*from.*keyPairs"
---

<objective>
Build the Instance Launch Wizard — a 4-step Cloudscape Wizard form for creating new EC2 instances. Dropdowns for VPC, Subnet, Security Group, and Key Pair are populated from live API calls using canonical hooks from Plans 04 and 05.

Purpose: Fulfills EC2-06 (instance creation with type/VPC/subnet/SG selection) and CRUD-03 (create resource via form).
Output: Working instance launch flow accessible from the Instances tab.

NOTE: This plan runs in Wave 3 (after Plans 04 and 05) so it can import the canonical useVpcs, useSubnets, useSecurityGroups, and useKeyPairs hooks directly. No temporary/duplicate hooks needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ec2-dashboard-crud-patterns/02-CONTEXT.md
@.planning/phases/02-ec2-dashboard-crud-patterns/02-RESEARCH.md
@.planning/phases/02-ec2-dashboard-crud-patterns/02-UI-SPEC.md
@.planning/phases/02-ec2-dashboard-crud-patterns/02-02-SUMMARY.md
@.planning/phases/02-ec2-dashboard-crud-patterns/02-04-SUMMARY.md
@.planning/phases/02-ec2-dashboard-crud-patterns/02-05-SUMMARY.md

<interfaces>
From web/src/services/ec2/api/ec2Client.ts:
```typescript
export async function ec2Query(action: string, params?: Record<string, string>): Promise<string>
export function addMemberList(params: Record<string, string>, key: string, ids: string[]): void
```

From web/src/shared/api/xml.ts:
```typescript
export function parseXml(text: string): Document
export function getText(el: Element, tag: string): string
export function getItems(parent: Document | Element, setTag: string): Element[]
```

From web/src/services/ec2/api/vpcs.ts (Plan 04 — canonical):
```typescript
export function useVpcs(): UseQueryResult<Ec2Vpc[]>
```

From web/src/services/ec2/api/subnets.ts (Plan 04 — canonical):
```typescript
export function useSubnets(): UseQueryResult<Ec2Subnet[]>
```

From web/src/services/ec2/api/securityGroups.ts (Plan 04 — canonical):
```typescript
export function useSecurityGroups(): UseQueryResult<Ec2SecurityGroup[]>
```

From web/src/services/ec2/api/keyPairs.ts (Plan 05 — canonical):
```typescript
export function useKeyPairs(): UseQueryResult<Ec2KeyPair[]>
```

From Plan 01 copy.ts (wizard entries):
```typescript
ec2WizardHeader: 'Launch an instance'
ec2WizardCancel: 'Discard and exit'
ec2WizardSubmit: 'Launch instance'
// step titles array
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AMI/Images API hook and RunInstances mutation</name>
  <files>web/src/services/ec2/api/images.ts, web/src/services/ec2/api/instances.ts</files>
  <read_first>
    web/src/services/ec2/api/ec2Client.ts,
    web/src/services/ec2/api/instances.ts,
    web/src/shared/api/xml.ts,
    .planning/phases/02-ec2-dashboard-crud-patterns/02-RESEARCH.md
  </read_first>
  <action>
1. Create `web/src/services/ec2/api/images.ts`:
   - Define type `Ec2Image = { imageId: string; name: string; description: string; architecture: string; platform: string }`
   - `parseImages(xml: string): Ec2Image[]` — parses DescribeImages XML. Images in `imagesSet > item`.
   - `useImages()` hook — queryKey `['ec2', 'images']`, staleTime 300_000 (5 min)

2. Add to `web/src/services/ec2/api/instances.ts`:
   - `useRunInstance()` mutation hook:
     - `mutationFn` accepts `{ imageId: string; instanceType: string; subnetId: string; securityGroupIds: string[]; keyName?: string; nameTag?: string }`
     - Builds params: `ImageId`, `InstanceType`, `SubnetId`, `MinCount: '1'`, `MaxCount: '1'`
     - If keyName: `params.KeyName = keyName`
     - If securityGroupIds: `addMemberList(params, 'SecurityGroupId', securityGroupIds)`
     - If nameTag: `params['TagSpecification.1.ResourceType'] = 'instance'`, `params['TagSpecification.1.Tag.1.Key'] = 'Name'`, `params['TagSpecification.1.Tag.1.Value'] = nameTag`
     - Calls `ec2Query('RunInstances', params)`
     - `onSuccess`: invalidate `['ec2', 'instances']`

   NOTE: Do NOT create temporary wizard hooks for VPCs/Subnets/SGs/KeyPairs. Import the canonical hooks from their respective API files (vpcs.ts, subnets.ts, securityGroups.ts, keyPairs.ts) which are available because this plan depends on Plans 04 and 05.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/minsu/workspace/ministack/web && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>AMI list hook and RunInstance mutation compile and are ready for wizard consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Instance Launch Wizard page and route</name>
  <files>web/src/services/ec2/pages/InstanceWizard.tsx, web/src/app/routes.tsx</files>
  <read_first>
    web/src/services/ec2/api/instances.ts,
    web/src/services/ec2/api/images.ts,
    web/src/services/ec2/api/vpcs.ts,
    web/src/services/ec2/api/subnets.ts,
    web/src/services/ec2/api/securityGroups.ts,
    web/src/services/ec2/api/keyPairs.ts,
    web/src/services/ec2/components/FlashNotifications.tsx,
    web/src/app/routes.tsx,
    .planning/phases/02-ec2-dashboard-crud-patterns/02-UI-SPEC.md
  </read_first>
  <action>
1. Create `web/src/services/ec2/pages/InstanceWizard.tsx` (default export):
   - Uses Cloudscape `Wizard` component with 4 steps
   - Import canonical hooks: `useVpcs` from `../api/vpcs`, `useSubnets` from `../api/subnets`, `useSecurityGroups` from `../api/securityGroups`, `useKeyPairs` from `../api/keyPairs`

   **Step 1: Name and instance type** (per UI-SPEC Wizard Contract)
   - FormField "Name" — Input, optional, max 255 chars
   - FormField "Instance type" — Select, required. Options: t2.micro, t2.small, t2.medium, t2.large, t3.micro, t3.small, t3.medium, t3.large, m5.large, m5.xlarge

   **Step 2: Network settings**
   - FormField "VPC" — Select, required. Options from `useVpcs()`, display: `${vpc.vpcId} (${vpc.cidrBlock})${vpc.nameTag ? ' — ' + vpc.nameTag : ''}`
   - FormField "Subnet" — Select, required. Options filtered by selected VPC from `useSubnets()`
   - FormField "Auto-assign public IP" — Select with Enable/Disable
   - When VPC changes: clear subnetId

   **Step 3: Security**
   - FormField "Security groups" — Multiselect, at least 1 required. Options filtered by selected VPC from `useSecurityGroups()`
   - FormField "Key pair" — Select, optional. Options from `useKeyPairs()`
   - Validate: at least 1 security group selected

   **Step 4: Review and launch**
   - Read-only summary using ColumnLayout + KeyValuePairs

   **Wizard behavior:**
   - `onCancel`: navigate to `/services/ec2/instances`
   - `onSubmit`: call `useRunInstance().mutateAsync(...)`. On success: Flashbar + navigate back. On error: Flashbar + stay on review.
   - Copy strings from `copy.ts` wizard entries

2. Update `web/src/app/routes.tsx`:
   - Add lazy import for InstanceWizard
   - Add route `{ path: 'launch-wizard', element: withSuspense(<InstanceWizard />) }` as child of `services/ec2`
  </action>
  <verify>
    <automated>cd /mnt/c/Users/minsu/workspace/ministack/web && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>4-step Instance Launch Wizard renders with live VPC/Subnet/SG/KeyPair dropdowns (canonical imports from Plans 04/05), validates required fields per step, creates instance via RunInstances, and navigates back to Instances tab on success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form -> RunInstances | User-provided form values sent to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Injection | RunInstances form values | accept | Values are sent as URLSearchParams to localhost. No SQL, no shell. Backend validates parameter format. Local dev tool. |
| T-02-10 | Spoofing | Wizard API data population | accept | VPC/Subnet/SG lists come from local backend. No external trust boundary. |
</threat_model>

<verification>
1. `cd web && npx tsc --noEmit` exits 0
2. `cd web && npx vitest run` — all tests pass
3. Navigate to /_console/services/ec2/launch-wizard — 4-step wizard renders
4. VPC/Subnet/SG/KeyPair dropdowns populated from live API (canonical hooks)
5. Launch instance creates instance and redirects to Instances tab
</verification>

<success_criteria>
- Wizard renders 4 steps with correct titles per UI-SPEC
- Instance type dropdown has 10 options
- VPC/Subnet/SG/KeyPair dropdowns populated from canonical hooks (no temporary hooks)
- Subnet filters by VPC, SG filters by VPC
- Required field validation prevents advancing without filling
- RunInstances call succeeds and instance appears in list
- Cancel returns to Instances tab without side effects
</success_criteria>

<output>
After completion, create `.planning/phases/02-ec2-dashboard-crud-patterns/02-07-SUMMARY.md`
</output>
