---
phase: 04-lambda-service
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - web/src/services/lambda/__tests__/fixtures.ts
  - web/src/services/lambda/__tests__/msw-handlers.ts
  - web/src/services/lambda/api/lambdaClient.test.ts
  - web/src/services/lambda/api/invokeClient.test.ts
  - web/src/services/lambda/api/codeUploadClient.test.ts
  - web/src/services/lambda/api/lambdaKeys.test.ts
  - web/src/services/lambda/api/useFunctions.test.ts
  - web/src/services/lambda/api/useFunction.test.ts
  - web/src/services/lambda/api/useEventSourceMappings.test.ts
  - web/src/services/lambda/api/useFunctionUrl.test.ts
  - web/src/services/lambda/api/functionMutations.test.ts
  - web/src/services/lambda/api/invokeMutation.test.ts
  - web/src/services/lambda/components/FunctionTable.test.tsx
  - web/src/services/lambda/components/CreateFunctionModal.test.tsx
  - web/src/services/lambda/components/DeleteFunctionModal.test.tsx
  - web/src/services/lambda/components/ConfigurationPanel.test.tsx
  - web/src/services/lambda/components/EnvironmentPanel.test.tsx
  - web/src/services/lambda/components/TriggersPanel.test.tsx
  - web/src/services/lambda/components/InvokePanel.test.tsx
  - web/src/services/lambda/components/PayloadEditor.test.tsx
  - web/src/services/lambda/components/InvokeResult.test.tsx
  - web/src/services/lambda/components/RelativeTime.test.tsx
  - web/src/services/lambda/FunctionListPage.test.tsx
  - web/src/services/lambda/FunctionDetailPage.test.tsx
autonomous: true
requirements:
  - LAM-01
  - LAM-02
  - LAM-03

must_haves:
  truths:
    - "Test stub files exist for every Lambda API helper, hook, mutation, page, and component named in RESEARCH §File Inventory"
    - "MSW handlers return valid JSON for ListFunctions, CreateFunction, GetFunction, UpdateFunctionConfiguration, DeleteFunction, Invoke (success + function-error), ListEventSourceMappings, ListFunctionUrlConfigs"
    - "Invoke fixtures attach X-Amz-Log-Result (base64 UTF-8 with multi-byte chars) and X-Amz-Function-Error headers per RESEARCH §Invoke Header Semantics"
    - "JSON fixtures use the HTTP-layer response shape (not internal executor shape) per Pitfall 4"
    - "All test stubs use test.todo() — vitest reports them as todo, not failing, not passing silently"
    - "Running `cd web && npm run test -- src/services/lambda --run` exits 0 with only todo tests"
  artifacts:
    - path: "web/src/services/lambda/__tests__/fixtures.ts"
      provides: "Lambda JSON response fixtures consumed by MSW handlers and unit tests"
      exports: ["LAMBDA_FIXTURES"]
    - path: "web/src/services/lambda/__tests__/msw-handlers.ts"
      provides: "MSW http handlers covering Lambda REST endpoints the UI calls"
      exports: ["lambdaHandlers"]
  key_links:
    - from: "web/src/services/lambda/__tests__/msw-handlers.ts"
      to: "web/src/test/msw.ts"
      via: "mswServer.use(...lambdaHandlers) in component tests"
      pattern: "lambdaHandlers"
    - from: "web/src/services/lambda/api/*.test.ts"
      to: "web/src/services/lambda/__tests__/fixtures.ts"
      via: "import { LAMBDA_FIXTURES }"
      pattern: "LAMBDA_FIXTURES"
---

<objective>
Create Wave 0 test scaffolding for Phase 4 Lambda: MSW handlers for every Lambda REST endpoint the UI calls, shared JSON fixtures (XML-free), and `test.todo()` stub files for every module named in RESEARCH.md §File Inventory. Every later plan's `<verify>` command runs vitest against one of these stubs, so all must compile before production code begins.

Purpose: Satisfies Nyquist compliance — every requirement (LAM-01/02/03) gets a concrete automated command pointing at a real file. Produces the mock + fixture infrastructure Plans 01–05 depend on. Locks the invoke-response shape (header-based error signal per Pitfall 2, base64-encoded UTF-8 logs per Pitfall 1) so all downstream tests speak the same protocol.
Output: MSW handlers, fixtures file, 22 test files containing `describe` + `test.todo()` stubs that compile under strict TypeScript.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-lambda-service/04-CONTEXT.md
@.planning/phases/04-lambda-service/04-RESEARCH.md
@.planning/phases/03-s3-lambda-services/03-00-PLAN.md
@.planning/phases/03-s3-lambda-services/03-00-SUMMARY.md
@web/src/services/s3/__tests__/fixtures.ts
@web/src/services/s3/__tests__/msw-handlers.ts

<interfaces>
<!-- Phase 3 established the Wave 0 pattern. Lambda mirrors it — but JSON not XML. -->

Phase 3 fixtures pattern (from web/src/services/s3/__tests__/fixtures.ts):
  export const S3_FIXTURES = { listBucketsEmpty: '<xml>...', ... }

Lambda equivalent (this plan):
  export const LAMBDA_FIXTURES = { listFunctionsEmpty: { Functions: [] }, ... }  // JSON objects, not XML strings

MSW pattern (from web/src/services/s3/__tests__/msw-handlers.ts):
  import { http, HttpResponse } from 'msw'
  export const s3Handlers = [ http.get('*/', ...) ]

Backend JSON shapes (VERBATIM from ministack/services/lambda_svc.py — RESEARCH §Backend REST Inventory):

ListFunctions (→ GET /2015-03-31/functions):
  { "Functions": [ { "FunctionName": "...", "Runtime": "...", "Handler": "...", "CodeSize": 1234,
                     "LastModified": "2026-04-17T10:30:00Z", "MemorySize": 128, "Timeout": 3,
                     "Role": "arn:aws:iam::000000000000:role/lambda-role",
                     "Architectures": ["x86_64"], "PackageType": "Zip", "State": "Active",
                     "Environment": {"Variables": {}}, "Version": "$LATEST",
                     "FunctionArn": "arn:aws:lambda:us-east-1:000000000000:function:...",
                     "RevisionId": "<uuid>", "EphemeralStorage": {"Size": 512} } ],
    "NextMarker": "<last-function-name>"  // only when truncated
  }

GetFunction (→ GET /2015-03-31/functions/{name}):
  { "Configuration": { /* same shape as Functions[i] */ },
    "Code": { "RepositoryType": "S3", "Location": "" },
    "Tags": {} }

Invoke (→ POST /2015-03-31/functions/{name}/invocations):
  Response status: 200 (even on function-error — Pitfall 2)
  Response headers:
    X-Amz-Executed-Version: $LATEST
    X-Amz-Log-Result: <base64 utf-8 — MUST include a multi-byte sequence like "안녕" per Pitfall 1>
    X-Amz-Function-Error: Unhandled  // only on error path
  Response body:
    success: JSON.stringify(function's return value), e.g. `{"statusCode":200,"body":"hello"}`
    error:   `{"errorMessage":"division by zero","errorType":"Runtime.HandlerError"}`

ListEventSourceMappings (→ GET /2015-03-31/event-source-mappings?FunctionName=...):
  Note the EXACT query key: `FunctionName` (not `Function`) per Pitfall 7.
  { "EventSourceMappings": [ { "UUID": "...", "FunctionArn": "...",
                               "EventSourceArn": "arn:aws:sqs:...", "State": "Enabled",
                               "BatchSize": 10, "LastModified": 1713360000.0,
                               "LastProcessingResult": "OK" } ],
    "NextMarker": "<last-uuid>" }

ListFunctionUrlConfigs (→ GET /2021-10-31/functions/{name}/urls):
  { "FunctionUrlConfigs": [ { "FunctionUrl": "https://<uuid>.lambda-url.us-east-1.on.aws/",
                              "FunctionArn": "...", "AuthType": "NONE", "Cors": {},
                              "CreationTime": "...", "LastModifiedTime": "..." } ] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Lambda JSON fixtures and MSW handlers</name>
  <files>web/src/services/lambda/__tests__/fixtures.ts, web/src/services/lambda/__tests__/msw-handlers.ts</files>
  <read_first>
    - web/src/services/s3/__tests__/fixtures.ts (mirror the export pattern — but JSON objects, not XML strings)
    - web/src/services/s3/__tests__/msw-handlers.ts (mirror the MSW v2 handler pattern)
    - .planning/phases/04-lambda-service/04-RESEARCH.md §"Backend REST Inventory" for exact JSON shapes (lines 106-290)
    - .planning/phases/04-lambda-service/04-RESEARCH.md §"Invoke Header Semantics" (lines 292-342) — header names, base64 UTF-8 decoding
    - .planning/phases/04-lambda-service/04-RESEARCH.md §"New Phase-4-Specific Pitfalls" 1, 2, 4, 7 — invoke fixture MUST speak HTTP-layer shape with headers, log base64 MUST contain multi-byte sequences
    - ministack/services/lambda_svc.py lines 322-386 (_build_config), 748-767 (_list_functions), 874-945 (_invoke), 2377-2410 (_list_esms), 2764 (_list_function_url_configs) to confirm response formats
  </read_first>
  <action>
    Create `web/src/services/lambda/__tests__/fixtures.ts` exporting `LAMBDA_FIXTURES` as a const object with keys:

    - `configBase` — a helper object conforming to `_build_config` output (used to build list + detail fixtures). Must include `FunctionName: 'hello'`, `Runtime: 'python3.12'`, `Handler: 'index.handler'`, `CodeSize: 1234`, `LastModified: '2026-04-17T10:30:00Z'`, `Timeout: 3`, `MemorySize: 128`, `Role: 'arn:aws:iam::000000000000:role/lambda-role'`, `Architectures: ['x86_64']`, `PackageType: 'Zip'`, `State: 'Active'`, `LastUpdateStatus: 'Successful'`, `Version: '$LATEST'`, `Description: ''`, `Environment: { Variables: { LOG_LEVEL: 'info', API_KEY: 'abc123' } }`, `Layers: []`, `TracingConfig: { Mode: 'PassThrough' }`, `RevisionId: '00000000-0000-0000-0000-000000000000'`, `EphemeralStorage: { Size: 512 }`, `FunctionArn: 'arn:aws:lambda:us-east-1:000000000000:function:hello'`, `LoggingConfig: { LogFormat: 'Text', LogGroup: '/aws/lambda/hello' }`.
    - `listFunctionsEmpty` — `{ Functions: [] }`
    - `listFunctionsTwo` — `{ Functions: [configBase, {...configBase, FunctionName: 'goodbye', Runtime: 'nodejs20.x', Handler: 'index.handler', LastModified: '2026-04-15T08:00:00Z', FunctionArn: 'arn:aws:lambda:us-east-1:000000000000:function:goodbye'}] }`
    - `listFunctionsTruncated` — `{ Functions: [configBase], NextMarker: 'hello' }`
    - `getFunctionHello` — `{ Configuration: configBase, Code: { RepositoryType: 'S3', Location: '' }, Tags: { team: 'platform' } }`
    - `getFunctionImage` — variant where `Configuration.PackageType='Image'` and `Code: { RepositoryType: 'ECR', ImageUri: 'public.ecr.aws/demo/hello:latest' }`
    - `createFunctionResponse` — the bare config object (same as `configBase` with `FunctionName: 'new-func'`). Returned with HTTP 201.
    - `updateConfigResponse` — config with `Timeout: 30, MemorySize: 512` (diff vs base).
    - `invokeSuccessBody` — `'{"statusCode":200,"body":"hello world"}'` (string — MSW will return as body)
    - `invokeSuccessLogBase64` — base64 of the UTF-8 string `'START RequestId: abc\n안녕 from Lambda\nEND RequestId: abc\n'`. Compute at module top level: `Buffer.from(...).toString('base64')` if Node; or inline the pre-computed base64 string. **Must contain non-ASCII bytes** so Pitfall 1 is exercised.
    - `invokeErrorBody` — `'{"errorMessage":"division by zero","errorType":"Runtime.HandlerError"}'`
    - `invokeErrorLogBase64` — base64 of `'START RequestId: xyz\nERROR division by zero\nEND RequestId: xyz\n'`
    - `esmsEmpty` — `{ EventSourceMappings: [] }`
    - `esmsOne` — `{ EventSourceMappings: [{ UUID: '11111111-2222-3333-4444-555555555555', FunctionArn: configBase.FunctionArn, FunctionName: 'hello', EventSourceArn: 'arn:aws:sqs:us-east-1:000000000000:my-queue', State: 'Enabled', Enabled: true, BatchSize: 10, LastModified: 1713360000.0, LastProcessingResult: 'OK' }] }`
    - `functionUrlEmpty` — `{ FunctionUrlConfigs: [] }`
    - `functionUrlOne` — `{ FunctionUrlConfigs: [{ FunctionUrl: 'https://abcdef.lambda-url.us-east-1.on.aws/', FunctionArn: configBase.FunctionArn, AuthType: 'NONE', Cors: {}, CreationTime: '2026-04-17T10:30:00Z', LastModifiedTime: '2026-04-17T10:30:00Z' }] }`

    All fixtures are plain JS objects (not XML strings).

    Create `web/src/services/lambda/__tests__/msw-handlers.ts` exporting `lambdaHandlers: HttpHandler[]` that covers:

      - `http.get('*/2015-03-31/functions')` → return `HttpResponse.json(LAMBDA_FIXTURES.listFunctionsTwo)`. If `request.url` has `?Marker=<any>` present, return `listFunctionsTruncated` so pagination tests can exercise it.
      - `http.post('*/2015-03-31/functions')` → return `HttpResponse.json(LAMBDA_FIXTURES.createFunctionResponse, { status: 201 })`.
      - `http.get('*/2015-03-31/functions/:name')` (not matching `/invocations`, `/configuration`, `/code`, `/urls` children — use MSW's path-specificity; i.e. order the more specific handlers BEFORE this one) → return `LAMBDA_FIXTURES.getFunctionHello` for name `'hello'`, 404 otherwise.
      - `http.put('*/2015-03-31/functions/:name/configuration')` → 200 with `LAMBDA_FIXTURES.updateConfigResponse`.
      - `http.delete('*/2015-03-31/functions/:name')` → 204 empty body; return 404 for name `'missing'`.
      - `http.post('*/2015-03-31/functions/:name/invocations')`:
        - default (name !== 'boom'): `new HttpResponse(LAMBDA_FIXTURES.invokeSuccessBody, { status: 200, headers: { 'Content-Type': 'application/json', 'X-Amz-Executed-Version': '$LATEST', 'X-Amz-Log-Result': LAMBDA_FIXTURES.invokeSuccessLogBase64 } })`
        - name === 'boom': `new HttpResponse(LAMBDA_FIXTURES.invokeErrorBody, { status: 200, headers: { 'Content-Type': 'application/json', 'X-Amz-Executed-Version': '$LATEST', 'X-Amz-Log-Result': LAMBDA_FIXTURES.invokeErrorLogBase64, 'X-Amz-Function-Error': 'Unhandled' } })`
      - `http.get('*/2015-03-31/event-source-mappings')`:
        - If `searchParams.get('FunctionName') === 'hello'` → `esmsOne`
        - else → `esmsEmpty`
      - `http.get('*/2021-10-31/functions/:name/urls')`:
        - For `:name === 'hello'` → `functionUrlOne`
        - else → `functionUrlEmpty`

    Use MSW v2 API (`http.get`, `HttpResponse.json(...)` for bodied responses; `new HttpResponse(string, { status, headers })` when header control is needed — specifically the invoke handlers). Do NOT touch `web/src/test/msw.ts` — that's the downstream plans' job.

    Every locked decision in CONTEXT.md is honored by these fixtures (D-02 three code sources supported by generic create handler; D-05 env vars in plaintext in config; D-07 ESM + function URL read-only shape; D-06 FunctionError signalled via header per Pitfall 2; D-10 `LastModified` ISO string present).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -cE "services/lambda/__tests__" ; cd web && test -f src/services/lambda/__tests__/fixtures.ts && test -f src/services/lambda/__tests__/msw-handlers.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const LAMBDA_FIXTURES" web/src/services/lambda/__tests__/fixtures.ts` returns 1
    - `grep -c "export const lambdaHandlers" web/src/services/lambda/__tests__/msw-handlers.ts` returns 1
    - `grep -cE "listFunctionsEmpty|listFunctionsTwo|listFunctionsTruncated|getFunctionHello|invokeSuccessBody|invokeErrorBody|invokeSuccessLogBase64|esmsOne|functionUrlOne" web/src/services/lambda/__tests__/fixtures.ts` >= 9
    - `grep -c "X-Amz-Log-Result" web/src/services/lambda/__tests__/msw-handlers.ts` >= 1   (Pitfall 1 hook point)
    - `grep -c "X-Amz-Function-Error" web/src/services/lambda/__tests__/msw-handlers.ts` >= 1   (Pitfall 2)
    - `grep -cE "FunctionName" web/src/services/lambda/__tests__/msw-handlers.ts` >= 1   (Pitfall 7 — ESM filter param)
    - `grep -cE "http\.(get|put|post|delete)" web/src/services/lambda/__tests__/msw-handlers.ts` >= 8
    - `cd web && npx tsc --noEmit -p tsconfig.json` reports zero errors under `web/src/services/lambda/**`
  </acceptance_criteria>
  <done>Fixtures file exports LAMBDA_FIXTURES with all documented keys (no XML anywhere — pure JSON); msw-handlers.ts exports lambdaHandlers covering List/Create/Get/Update/Delete/Invoke(success+error)/ListESMs/ListFunctionUrls with correct headers and status codes. Files compile under strict TS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stub test files for all Lambda modules (todo tests, no implementation)</name>
  <files>web/src/services/lambda/api/lambdaClient.test.ts, web/src/services/lambda/api/invokeClient.test.ts, web/src/services/lambda/api/codeUploadClient.test.ts, web/src/services/lambda/api/lambdaKeys.test.ts, web/src/services/lambda/api/useFunctions.test.ts, web/src/services/lambda/api/useFunction.test.ts, web/src/services/lambda/api/useEventSourceMappings.test.ts, web/src/services/lambda/api/useFunctionUrl.test.ts, web/src/services/lambda/api/functionMutations.test.ts, web/src/services/lambda/api/invokeMutation.test.ts, web/src/services/lambda/components/FunctionTable.test.tsx, web/src/services/lambda/components/CreateFunctionModal.test.tsx, web/src/services/lambda/components/DeleteFunctionModal.test.tsx, web/src/services/lambda/components/ConfigurationPanel.test.tsx, web/src/services/lambda/components/EnvironmentPanel.test.tsx, web/src/services/lambda/components/TriggersPanel.test.tsx, web/src/services/lambda/components/InvokePanel.test.tsx, web/src/services/lambda/components/PayloadEditor.test.tsx, web/src/services/lambda/components/InvokeResult.test.tsx, web/src/services/lambda/components/RelativeTime.test.tsx, web/src/services/lambda/FunctionListPage.test.tsx, web/src/services/lambda/FunctionDetailPage.test.tsx</files>
  <behavior>
    Each file contains a `describe('{moduleName}', () => { ... })` with at least 2 `test.todo()` entries naming the behavior to be implemented. Vitest MUST report them as todo (not passing, not failing).
  </behavior>
  <read_first>
    - web/src/services/s3/api/s3Client.test.ts (Plan 03-00 todo stub template — mirror its shape)
    - .planning/phases/04-lambda-service/04-RESEARCH.md §"File / Module Inventory" (lines 808-886) — lists required behaviors per file
    - .planning/phases/04-lambda-service/04-RESEARCH.md §"New Phase-4-Specific Pitfalls" — todo labels must name the pitfalls they guard against
    - .planning/phases/04-lambda-service/04-CONTEXT.md §"Locked Decisions" D-01..D-10 — every decision must map to at least one test.todo
  </read_first>
  <action>
    For each file listed in `<files>`, create a minimal test stub importing `{ describe, test }` from `vitest`. No production imports (modules don't exist yet). Required `test.todo()` entries per file (verbatim labels — they must be greppable):

    1. `api/lambdaClient.test.ts` [LAM-01, LAM-03]:
       - `test.todo('lambdaGet assembles path against window origin')`
       - `test.todo('lambdaPost attaches dummy Authorization header and Content-Type: application/json')`
       - `test.todo('lambdaPut serializes body with JSON.stringify')`
       - `test.todo('lambdaDelete issues DELETE and resolves on 204')`
       - `test.todo('lambdaGet encodes searchParams correctly')`

    2. `api/invokeClient.test.ts` [LAM-02]:
       - `test.todo('invokeFunction sends X-Amz-Invocation-Type: RequestResponse and X-Amz-Log-Type: Tail headers')`
       - `test.todo('invokeFunction encodes function name in URL via encodeURIComponent (Pitfall 9 avoidance — no Qualifier)')`
       - `test.todo('decodeLogResult decodes base64 UTF-8 preserving multi-byte characters (Pitfall 1)')`
       - `test.todo('invokeFunction surfaces X-Amz-Function-Error header independently from HTTP status (Pitfall 2)')`
       - `test.todo('invokeFunction parses JSON body but retains raw string on parse failure (Pitfall 3)')`
       - `test.todo('invokeFunction exposes X-Amz-Executed-Version header')`

    3. `api/codeUploadClient.test.ts` [LAM-01 / D-02]:
       - `test.todo('fileToBase64 encodes a small zip to a base64 string matching btoa(binary) of bytes')`
       - `test.todo('fileToBase64 chunks large files at 32KB boundaries without exceeding call-stack argument limits')`
       - `test.todo('fileToBase64 preserves byte fidelity for UTF-8 binary content')`

    4. `api/lambdaKeys.test.ts` [LAM-01/03]:
       - `test.todo('lambdaKeys.functions(null) returns ["lambda","functions",null]')`
       - `test.todo('lambdaKeys.function(name) returns ["lambda","function", name]')`
       - `test.todo('lambdaKeys.triggers(name) returns ["lambda","triggers", name]')`
       - `test.todo('lambdaKeys.functionUrl(name) returns ["lambda","functionUrl", name]')`

    5. `api/useFunctions.test.ts` [LAM-01]:
       - `test.todo('useFunctions queryKey reflects marker argument')`
       - `test.todo('useFunctions parses Functions[] from ListFunctions response')`
       - `test.todo('useFunctions placeholderData preserves previous page during refetch (marker pagination)')`
       - `test.todo('useFunctions forwards MaxItems query param')`

    6. `api/useFunction.test.ts` [LAM-03]:
       - `test.todo('useFunction returns Configuration, Code, Tags')`
       - `test.todo('useFunction surfaces 404 for missing function')`
       - `test.todo('useFunction disables query when name is empty')`

    7. `api/useEventSourceMappings.test.ts` [LAM-03 / Pitfall 7]:
       - `test.todo('useEventSourceMappings sends FunctionName query param (not Function — Pitfall 7)')`
       - `test.todo('useEventSourceMappings returns empty array when no mappings configured')`
       - `test.todo('useEventSourceMappings returns ESMs with UUID, State, BatchSize, EventSourceArn, LastProcessingResult')`

    8. `api/useFunctionUrl.test.ts` [LAM-03 / D-07]:
       - `test.todo('useFunctionUrl queries ListFunctionUrlConfigs (plural) not Get (singleton)')`
       - `test.todo('useFunctionUrl returns empty array for unconfigured function (not 404)')`
       - `test.todo('useFunctionUrl exposes AuthType and FunctionUrl')`

    9. `api/functionMutations.test.ts` [LAM-01 / D-01 / D-02]:
       - `test.todo('useCreateFunction with ZipFile source POSTs Code.ZipFile as base64')`
       - `test.todo('useCreateFunction with S3 source POSTs Code.S3Bucket + Code.S3Key')`
       - `test.todo('useCreateFunction with ImageUri source POSTs Code.ImageUri and sets PackageType: Image')`
       - `test.todo('useCreateFunction invalidates ["lambda","functions"] on success (prefix match — Pitfall C-3)')`
       - `test.todo('useDeleteFunction DELETEs /functions/{name} and invalidates ["lambda","functions"]')`
       - `test.todo('useDeleteFunction never sends a Qualifier query param (Pitfall 9)')`
       - `test.todo('useUpdateConfiguration PUTs whitelisted keys and invalidates ["lambda","function", name] only (Pitfall C-3)')`

    10. `api/invokeMutation.test.ts` [LAM-02 / Pitfall 6]:
        - `test.todo('useInvoke calls invokeFunction with name and payload')`
        - `test.todo('useInvoke does NOT invalidate any query on success (Pitfall 6 — invoke is cache-neutral)')`
        - `test.todo('useInvoke surfaces functionError on mutation success (not as throw)')`

    11. `components/FunctionTable.test.tsx` [LAM-01]:
        - `test.todo('renders function name as Link navigating to /services/lambda/{name}')`
        - `test.todo('renders Runtime, Handler, CodeSize, Last modified columns')`
        - `test.todo('Last modified column uses RelativeTime component (D-10)')`
        - `test.todo('empty-state body matches copy.lambda.functionsEmptyBody')`
        - `test.todo('load-error Alert shows Retry button')`
        - `test.todo('row click (non-name) selects row for Delete action')`

    12. `components/CreateFunctionModal.test.tsx` [LAM-01 / D-02]:
        - `test.todo('Tiles/RadioGroup selects between Zip, S3, and Image code sources (D-02)')`
        - `test.todo('Zip source preselected by default (D-02)')`
        - `test.todo('only the relevant code-source inputs are shown for the active tile')`
        - `test.todo('submit with zip source calls fileToBase64 and posts Code.ZipFile')`
        - `test.todo('submit with S3 source posts Code.S3Bucket + Code.S3Key')`
        - `test.todo('submit with Image source posts Code.ImageUri and PackageType: Image')`
        - `test.todo('function name validation rejects names outside 1..64 chars and [a-zA-Z0-9-_]')`
        - `test.todo('submit button disabled while validation fails or mutation pending')`
        - `test.todo('accept=".zip" on file input is advisory only (Pitfall 5) — backend error surfaces via Flashbar')`

    13. `components/DeleteFunctionModal.test.tsx` [LAM-01]:
        - `test.todo('single-delete requires exact function-name match before enabling Delete')`
        - `test.todo('submit calls useDeleteFunction with function name')`
        - `test.todo('no Qualifier query param ever sent (Pitfall 9)')`

    14. `components/ConfigurationPanel.test.tsx` [LAM-03]:
        - `test.todo('renders Runtime, Handler, MemorySize, Timeout, CodeSize, Role ARN, Architectures, Last modified')`
        - `test.todo('Role ARN has CopyToClipboard button')`
        - `test.todo('Last modified uses RelativeTime (D-10)')`
        - `test.todo('PackageType Image shows ImageUri row instead of CodeSize')`

    15. `components/EnvironmentPanel.test.tsx` [LAM-03 / D-05]:
        - `test.todo('renders env var key and value columns as plaintext — no masking (D-05)')`
        - `test.todo('no show/hide toggle rendered (D-05)')`
        - `test.todo('empty-state copy matches copy.lambda.environmentEmpty')`
        - `test.todo('columns sortable and filterable')`

    16. `components/TriggersPanel.test.tsx` [LAM-03 / D-07]:
        - `test.todo('renders ESM rows with UUID, source ARN, State, BatchSize, LastProcessingResult')`
        - `test.todo('renders Function URL entries with AuthType and URL')`
        - `test.todo('no create/edit/delete controls rendered (D-07 read-only)')`
        - `test.todo('empty state when neither ESMs nor Function URLs exist')`

    17. `components/InvokePanel.test.tsx` [LAM-02 / D-04 / D-09]:
        - `test.todo('Invoke button disabled while payload JSON is invalid (D-04)')`
        - `test.todo('clicking Invoke submits JSON.parse→JSON.stringify round-tripped payload (Pitfall 3)')`
        - `test.todo('inline Spinner with copy "Invoking... 10초+" appears while mutation pending (D-09)')`
        - `test.todo('after 3 seconds elapsed, spinner copy swaps to "Still invoking..." (D-09)')`
        - `test.todo('no Cancel button rendered (D-09)')`
        - `test.todo('InvokeResult renders after mutation success')`

    18. `components/PayloadEditor.test.tsx` [LAM-02 / D-04]:
        - `test.todo('invalid JSON shows FormField errorText "Invalid JSON: ..."')`
        - `test.todo('valid JSON clears the error')`
        - `test.todo('sample payload dropdown loads each of 4 templates into Textarea')`
        - `test.todo('no Monaco or CodeMirror imports (D-04 — Cloudscape Textarea only)')`

    19. `components/InvokeResult.test.tsx` [LAM-02 / D-06]:
        - `test.todo('Response section rendered above Logs section (D-06)')`
        - `test.todo('Response body pretty-printed via JSON.stringify(body, null, 2) inside <pre>')`
        - `test.todo('Logs section shows decoded UTF-8 text (multi-byte characters preserved — Pitfall 1)')`
        - `test.todo('FunctionError header presence renders red Alert above Response (D-06)')`
        - `test.todo('missing X-Amz-Log-Result renders "No logs" empty state')`
        - `test.todo('no tabs between Response and Logs (D-06 vertical stack)')`

    20. `components/RelativeTime.test.tsx` [D-10]:
        - `test.todo('renders relative copy like "3 minutes ago" via Intl.RelativeTimeFormat')`
        - `test.todo('title attribute exposes absolute ISO string on hover (D-10)')`
        - `test.todo('handles future dates, "just now", and very old dates')`

    21. `FunctionListPage.test.tsx` [LAM-01]:
        - `test.todo('renders FunctionTable fed by useFunctions')`
        - `test.todo('Create function button opens CreateFunctionModal')`
        - `test.todo('Delete action opens DeleteFunctionModal with selected function')`
        - `test.todo('route /services/lambda resolves to this page before /services/:serviceKey wildcard (Pitfall C-1)')`

    22. `FunctionDetailPage.test.tsx` [LAM-02 / LAM-03 / Pitfall 10]:
        - `test.todo('renders four tabs: Configuration, Environment, Triggers, Test (D-03)')`
        - `test.todo('404 from useFunction shows "Function not found" with back link')`
        - `test.todo('payload state is lifted to page level and persists across tab switches (Pitfall 10)')`
        - `test.todo('payload state resets when functionName route param changes (Open Question 3 resolved)')`

    Each stub file imports only `{ describe, test }` from `vitest`. No production imports (those modules don't exist yet). This makes the stubs compile cleanly.
  </action>
  <verify>
    <automated>cd web && npm run test -- src/services/lambda --run 2>&1 | grep -E "todo|Tests.*(passed|skipped)"</automated>
  </verify>
  <acceptance_criteria>
    - 22 test files exist (10 under api/, 10 under components/, 2 at services/lambda/ root)
    - `find web/src/services/lambda -name "*.test.*" | wc -l` >= 22
    - `grep -rc "test\.todo" web/src/services/lambda | awk -F: '{s+=$2} END {print s}'` returns >= 75
    - `cd web && npm run test -- src/services/lambda --run` exits 0 with all results marked "todo"
    - `cd web && npx tsc --noEmit -p tsconfig.json` reports zero errors under `web/src/services/lambda/**`
    - Zero `expect(` calls in any stub file (grep `expect(` across the 22 test files returns 0)
    - Every D-0N label (D-01..D-10) appears somewhere across the stub files: `grep -rcE "D-0[1-9]|D-10" web/src/services/lambda | awk -F: '{s+=$2} END {print s}'` >= 10
    - Pitfall tags present: `grep -rcE "Pitfall [1-9]|Pitfall 10|Pitfall C-[1-9]" web/src/services/lambda | awk -F: '{s+=$2} END {print s}'` >= 8
  </acceptance_criteria>
  <done>All 22 stub files exist, contain >= 75 total test.todo entries across LAM-01..03, compile under strict TS, and vitest reports them as todo when run. Every locked decision D-01..D-10 and every named pitfall is hooked by at least one todo label.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Wave 0 creates test infrastructure only. No runtime trust boundaries crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-00-01 | Tampering | MSW handler fixtures in test bundle | accept | Test-only code excluded from production build by vitest/vite. No user-facing surface. |
| T-4-00-02 | Information Disclosure | base64-encoded log fixtures containing fake "secret-shaped" strings | accept | Fixtures use obviously synthetic values (`abc123`, `hello world`, `안녕 from Lambda`). No real credentials. |
</threat_model>

<deviation_framework>
If the executor encounters friction implementing this plan:

- **Rule 1 (auto-fix trivia):** Typos in fixture keys, import path casing, missing semicolons, obvious TS errors from MSW v1 vs v2 API differences — fix silently and note in SUMMARY under "Rule 1 fixups".
- **Rule 2 (document deviations):** Additional fixture keys needed by downstream plans (e.g. a new function runtime variant), MSW path pattern adjustments for test isolation — implement, document in SUMMARY under "Rule 2 deviations" with one-line rationale.
- **Rule 3 (blocker — stop + ask):** Any MSW handler that cannot faithfully reproduce the backend's header behavior (X-Amz-Log-Result, X-Amz-Function-Error); any fixture that would force a downstream plan to skip a locked decision; any base64 encoding approach that breaks multi-byte UTF-8. Stop, open the blocker in SUMMARY, surface to human.
</deviation_framework>

<verification>
Running `cd web && npm run test -- src/services/lambda --run` exits 0 and reports all tests as todo. Running `cd web && npx tsc --noEmit -p tsconfig.json` reports zero errors.
</verification>

<success_criteria>
22 test files + fixtures + MSW handlers exist. Every downstream plan can reference a concrete test file in its `<verify>` block. Vitest infrastructure green before production code is written. Every CONTEXT decision D-01..D-10 and every named RESEARCH pitfall is hooked by at least one todo label.
</success_criteria>

<output>
After completion, create `.planning/phases/04-lambda-service/04-00-SUMMARY.md`
</output>
